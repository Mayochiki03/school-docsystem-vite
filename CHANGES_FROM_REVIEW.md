# สรุปการแก้ไขจากรีวิวโค้ด (school-docsystem-vite)

ไฟล์นี้สรุปทุกจุดที่แก้ไขแล้ว และจุดที่ยังต้องทำต่อ เทียบกับที่คุณแจ้งมา

## แก้ไขแล้ว

### 1. โหมดสี (มืด/สว่าง) แสดงผิดตามที่ตั้งไว้
**ไฟล์:** `web/src/index.css`
**สาเหตุจริง:** โปรเจกต์ใช้ Tailwind CSS v4 ซึ่งเปลี่ยนพฤติกรรมของ `dark:` ให้อิงตาม
การตั้งค่าโหมดมืดของ "เบราว์เซอร์/OS ผู้ใช้" โดยอัตโนมัติ แทนที่จะอิงตาม class `.dark`
บน `<html>` เหมือน v3 — ทำให้ `ThemeContext` ที่สลับ class `.dark` ไม่มีผลกับ
`dark:text-...`, `dark:bg-...` เลย สีเลยดูขัดกันไปมาตามเครื่องแต่ละคน
**วิธีแก้:** เพิ่ม `@custom-variant dark (&:where(.dark, .dark *));` ต่อจาก
`@import "tailwindcss";` บรรทัดเดียว ทำให้ `dark:` กลับมาอิงตาม class ที่แอปสลับให้จริง
ทั้งหมด — นี่คือจุดเดียวที่แก้ปัญหาตัวอักษรอ่านไม่ออกและปัญหาธีมกำหนดเองไม่ค่อยมีผล
ได้เกือบทั้งหมด

### 2. ข้อความทะลุกรอบ/ทะลุขอบกระดาษ
**ไฟล์:** `web/src/pages/documents/DocumentDetail.tsx`
- ช่องแสดงค่าฟิลด์ในหน้ารายละเอียดเอกสาร: เพิ่ม `break-words` (เดิมมีแค่
  `whitespace-pre-wrap` ซึ่งไม่ตัดคำที่ไม่มีช่องว่างเลย)
- ผังหน้ากระดาษแบบลากวาง (`PrintLayoutView`): เดิมใช้ `whitespace-nowrap` ไม่มี
  การจำกัดความกว้าง ข้อความยาวจึงทะลุขอบกระดาษได้เสมอ ไม่ว่าจะวางตำแหน่งไหน ตอนนี้
  คำนวณ `max-width` จากระยะถึงขอบกระดาษที่ใกล้ที่สุด และให้ตัดคำได้

### 3. บัญชียามเห็นเมนูทั้งหมด (ควรเห็นแค่ 2 เมนู)
**ไฟล์:** `web/src/components/Layout.tsx`
เดิมไม่มีการเช็ค role ยามเลยในเมนู ใช้ `show: true` แบบไม่มีเงื่อนไขในหลายเมนูหลัก
ตอนนี้ถ้า role เป็น `guard` จะเห็นเฉพาะ "เช็คลิสต์เวรยาม" และ "บันทึกรถเข้า-ออก"
เหมือนระบบเดิม ไม่เห็นเมนูแดชบอร์ด/เอกสาร/แอดมินใดๆ เลย

### 4. ตัวเปิดปิด "เช็คลิสต์เวรยาม" ซ้ำกันอยู่ 2 ที่ (คนละระบบ ไม่เชื่อมกัน)
**ไฟล์:** `web/src/pages/admin/AdminSettings.tsx`
หน้า "ตั้งค่าระบบ" มีอีกตัวแปรเก่า `settings.guardChecklistEnabled` ที่ backend
ไม่ได้ใช้บังคับสิทธิ์จริงแล้ว (ของจริงคือ `featureToggles.guardChecklist` ในหน้า
"เปิด/ปิดเมนูฟังก์ชัน") จึงลบตัวซ้ำที่ไม่มีผลออกจากหน้าตั้งค่าระบบ เหลือจุดควบคุม
เดียวที่หน้าเปิด/ปิดเมนูฟังก์ชัน

### 5. ระบบ Preview/พิมพ์แบบ A4 หายไป (คืนความสามารถจากของเดิม)
**ไฟล์:** `web/src/pages/documents/DocumentDetail.tsx`, `web/src/index.css`,
`web/index.html`
เดิมกดพิมพ์แล้ว `window.print()` จะพิมพ์ "ทั้งหน้าเว็บ" ตรงๆ ไม่มีฟอร์ม A4 ไม่มี
หน้าต่าง preview เลย ตอนนี้คืนกลับมา:
- เพิ่ม `<div id="printArea">` ถาวรใน `index.html` และ CSS `@media print` แบบเดิม
  ที่ซ่อนทุกอย่างยกเว้น `#printArea` ตอนสั่งพิมพ์
- สร้างคอมโพเนนต์ `ClassicMemoSheet` — ฟอร์มบันทึกข้อความ A4 แบบเดิม (หัวกระดาษ
  ชื่อโรงเรียน / ที่-วันที่ / เรื่อง-เรียน / เนื้อหา / บล็อกลงชื่อผู้จัดทำมุมขวาล่าง
  + บล็อกความเห็นผู้เกษียนมุมซ้ายล่างถ้ามี) ใช้เป็นค่าเริ่มต้นสำหรับทุกประเภทเอกสาร
  ที่ยังไม่ได้ตั้งผังหน้ากระดาษแบบลากวางไว้
- เพิ่มปุ่ม "ดูตัวอย่าง/พิมพ์" เปิดหน้าต่าง preview ขนาด A4 ย่อบนหน้าจอ พร้อมปุ่ม
  สั่งพิมพ์จริงในตัว
- ถ้าประเภทเอกสารมีการตั้งผังหน้ากระดาษแบบลากวางไว้แล้ว (ฟีเจอร์ใหม่) ระบบจะยังใช้
  ผังนั้นแทนได้ตามเดิม ไม่ได้ตัดฟีเจอร์ใหม่ทิ้ง แค่ทำให้ "ฟอร์มคลาสสิก" เป็นค่าเริ่มต้น
  แทนที่จะไม่มีอะไรเลย

## ยังไม่ได้แก้ / ต้องตัดสินใจเพิ่ม

### "วันที่หนังสือต้นทาง" เป็นข้อความธรรมดาไม่ใช่ตัวเลือกวันที่
ไม่ใช่บั๊กโค้ด — ฟิลด์นี้ถูกตั้งเป็นชนิด `text` แทนที่จะเป็น `date` ตอนสร้างประเภท
เอกสาร "หนังสือเข้าจากภายนอก" แก้ได้เองที่ Admin → ประเภทเอกสาร → แก้ไขฟิลด์นั้น
เปลี่ยนชนิดเป็น "วันที่"

### ระบบธีมกำหนดเอง (custom)
หลัง fix ข้อ 1 แล้ว ปุ่มเลือกสีหลัก/สีเน้นจะมีผลจริงกับปุ่มและองค์ประกอบที่อ้างอิง
`--color-primary`/`--color-accent` แต่สีพื้นหลัง/ตัวอักษรทั่วไป (เทาต่างๆ ของ
Tailwind) ยังไม่ถูกดึงไปอยู่ใน custom mode เพราะโค้ดส่วนใหญ่ hardcode เป็น
`slate-*` ไม่ใช่ CSS variable ถ้าต้องการให้ "กำหนดเอง" เปลี่ยนได้ทั้งหน้าจริงๆ
(ไม่ใช่แค่ปุ่ม) ต้องรื้อปรับให้ทุกที่อ้างอิง CSS variable แทน ซึ่งเป็นงานเพิ่มอีกก้อนหนึ่ง
แยกจากรอบนี้

### ไอคอนปุ่มเปิด/ปิดในหน้า "เปิด/ปิดเมนูฟังก์ชัน"
ตรวจสอบโค้ดแล้วเป็นสวิตช์ toggle มาตรฐาน (ทรงเดียวกับที่ใช้ทั่วทั้งแอป) ไม่พบ
โค้ดที่ผิดปกติ — รบกวนส่งภาพหน้าจอที่เห็นว่า "แปลกๆ" มาเพิ่ม เผื่อเป็นปัญหาเฉพาะ
เบราว์เซอร์/ธีมที่ยังไม่ได้ลองซ้ำ

### ยังไม่ได้ build จริงในเครื่องนี้
`npx tsc --noEmit` ผ่านสะอาด (ไม่มี type error) แต่ `vite build` ในแซนด์บ็อกซ์นี้
ติดปัญหา native binding ของ `rolldown` ที่ไม่เกี่ยวกับโค้ดที่แก้ (ปัญหาเครื่อง/แพลตฟอร์ม
ของแซนด์บ็อกซ์เอง) แนะนำให้รัน `npm install` แล้ว `npm run build` ในเครื่องคุณเองอีกที
ก่อนขึ้นโปรดักชัน
