# CHANGELOG

บันทึกการเปลี่ยนแปลงของระบบ — อัปเดตทุกครั้งที่มีการแก้ไข พร้อมเลขเวอร์ชัน
(เวอร์ชันปัจจุบันดูได้ที่ `server/version.js`, `web/src/version.ts` และหน้า "สถานะระบบ/Log")

> ไฟล์นี้รวมประวัติทั้งหมดของระบบไว้ที่เดียว (เดิมกระจายอยู่หลายไฟล์: CHANGELOG.md, CHANGELOG_THIS_ROUND.md,
> CHANGES_FROM_REVIEW.md, อ่านก่อน.md — รวมเข้าที่นี่แล้วเมื่อ v2.4.0 เพื่อให้หาประวัติการแก้ไขง่ายขึ้น
> เรียงจากใหม่สุดไปเก่าสุด

---

## v2.6.1 — 2026-09-18

**ทดสอบเพิ่มเติมและแก้ช่องโหว่ด้านพื้นที่ดิสก์ของฟีเจอร์ "เลือกหน้าจากไฟล์รวม" (v2.6.0)**

- **ทดสอบประสิทธิภาพกับไฟล์จริง 865 หน้า** — pdf.js โหลดไฟล์ทั้งหมดใช้เวลาแค่ ~164ms และเปิดดูหน้าไหนก็ได้แทบจะ
  ทันที (pdf.js อ่านแค่โครงสร้างหน้าที่ต้องใช้ ไม่ได้โหลดเนื้อหาทุกหน้าล่วงหน้า) ยืนยันว่า popup เลือกหน้าจะไม่หน่วง
  แม้ไฟล์จะมีหลายร้อยหน้าจริงๆ
- **แก้ปัญหาไฟล์กำพร้าค้างดิสก์** — เดิมทุกครั้งที่เลือกไฟล์ในฟิลด์ "แนบไฟล์เอกสาร" ระบบจะอัปโหลด+แปลงไฟล์ทันที
  (ก่อนกดบันทึกฟอร์ม) ถ้าผู้ใช้ปิดหน้าทิ้งกลางคันโดยไม่กดบันทึกต่อ ไฟล์ที่อัปโหลด/แปลงไปแล้ว (อาจหนักหลาย MB
  ถ้าเป็นไฟล์รวมหลายร้อยหน้า) จะค้างอยู่ในดิสก์ตลอดไปโดยไม่มีอะไรอ้างอิงถึง — เพิ่มงานลบไฟล์กำพร้าอัตโนมัติ
  (`server/lib/cleanupUploads.js`) รันทุก 6 ชั่วโมง ลบเฉพาะไฟล์ที่ไม่มีเอกสาร/ไฟล์แนบไหนอ้างอิงถึงแล้ว **และ**
  เก่ากว่า 6 ชั่วโมง (กันลบไฟล์ที่กำลังอยู่ระหว่างขั้นตอนบันทึกฟอร์มพอดี) — ทดสอบแล้วว่าแยกไฟล์กำพร้า/ไฟล์ที่ยังใช้
  งานอยู่/ไฟล์ใหม่ที่ยังไม่ถึงเวลาได้ถูกต้อง

ไฟล์ใหม่: `server/lib/cleanupUploads.js`
ไฟล์ที่แก้: `server/server.js` (เรียก `scheduleUploadCleanup` ตอนสตาร์ท)

---

## v2.6.0 — 2026-09-18

**แก้ path bug ใน `officeConvert.js`** (พบและแก้โดยผู้ใช้เอง) — `file://` ต้องมี `/` อีกตัวก่อน path บน Windows
(`file:///C:/...`) ไม่งั้น LibreOffice ตีความ `C:` เป็นชื่อ host แทนไดรฟ์ แล้วหาโฟลเดอร์โปรไฟล์ไม่เจอ

**เพิ่มฟีเจอร์ "เลือกหน้าจากไฟล์รวม"** — ตอบโจทย์กรณีโรงเรียนรวมคำสั่ง/หนังสือทั้งปีไว้ในไฟล์เดียว (200-400+ หน้า)
แล้วต้องหยิบเฉพาะหน้าที่เกี่ยวกับเอกสารฉบับนี้ออกมา:
- พอเลือกไฟล์ในฟิลด์ "แนบไฟล์เอกสาร" ระบบจะอัปโหลด+แปลงเป็น PDF ทันที (ไม่รอกดบันทึกฟอร์ม) แล้วเปิด popup
  ให้เลือกหน้าอัตโนมัติ
- เลือกหน้าได้หลายวิธี: คลิกทีละหน้า, shift+คลิกเลือกทั้งช่วง, หรือพิมพ์ช่วงหน้าตรงๆ เช่น "12-15, 20, 33"
  (จำเป็นมากเวลาต้องเลือกจากไฟล์ 200-400 หน้า) มีปุ่ม "เลือกทั้งหมด"/"ล้างที่เลือก" ด้วย
- รูปย่อแต่ละหน้า render แบบ lazy (โผล่เข้าใกล้จอถึง render) กันเบราว์เซอร์ค้างตอนเปิดไฟล์หลายร้อยหน้า
- กด "ใช้ N หน้าที่เลือก" แล้วระบบจะตัดเฉพาะหน้าที่เลือกออกมาเป็น PDF ใหม่ที่เบราว์เซอร์เอง (ด้วย pdf-lib ไม่ต้อง
  ส่งไฟล์ไปมาเซิร์ฟเวอร์หลายรอบ) — ทดสอบกับไฟล์ตัวอย่างจริงที่มี 865 หน้าแล้ว extract หน้าที่ต้องการออกมาได้ถูกต้อง
- ปรับ timeout การแปลงไฟล์ (LibreOffice) จาก 45 วินาที เป็น 180 วินาที และ timeout ฝั่งเบราว์เซอร์สำหรับ endpoint
  นี้โดยเฉพาะให้ยาวขึ้นตามด้วย (ไฟล์รวมหลายร้อยหน้าแปลงนานกว่าเอกสารสั้นๆ มาก) ขยายเพดานขนาดไฟล์แนบเป็น 60MB

**Endpoint ใหม่:** `POST /api/uploads/convert-preview` — อัปโหลด+แปลงไฟล์ให้พรีวิวได้ก่อนผูกกับเอกสารจริง
(รีแฟกเตอร์ตรรกะบันทึก/แปลงไฟล์เดิมใน `processFileContentFields` ออกมาเป็นฟังก์ชันกลาง `saveAndConvertUpload`
ใช้ร่วมกันทั้งจุดนี้และตอนบันทึกเอกสารจริง)

ไฟล์ใหม่: `web/src/components/PdfPageSelector.tsx`, `web/src/components/PdfPageThumb.tsx`, `web/src/lib/pdfjsSetup.ts`
ไฟล์ที่แก้: `server/lib/officeConvert.js`, `server/server.js`, `web/src/components/FileContentPicker.tsx`,
`web/src/api/client.ts` (รองรับ timeout ต่อ request)
Dependency ใหม่ (เฉพาะ web): `pdfjs-dist`, `pdf-lib`

---

## v2.5.1 — 2026-09-17 (แก้ด่วน)

**แก้หน้ารายละเอียดเอกสารพังทั้งหน้า (React error) เวลาเปิดเอกสารที่มีฟิลด์ "แนบไฟล์เอกสาร" (fileContent)**
- สาเหตุ: หน้าสรุปข้อมูลแบบย่อ และกระดาษพิมพ์ (MemoSheet) ยังไม่มีเงื่อนไขรองรับค่าฟิลด์ชนิดนี้ (ซึ่งเป็น object
  `{fileName, filePath, pdfPath, ...}`) พยายามเอาไป render ตรงๆ เป็นข้อความ ทำให้ React throw error "Objects are
  not valid as a React child" และทั้งหน้าแสดงผลไม่ได้เลย
- แก้แล้ว: หน้าสรุปย่อแสดงแค่ชื่อไฟล์สั้นๆ (ตัวอย่าง PDF เต็มๆ อยู่ด้านบนหน้าอยู่แล้ว), กระดาษพิมพ์ข้ามฟิลด์นี้ไปเฉยๆ
  (เพราะเนื้อหาจริงพิมพ์จากตัวอย่าง PDF โดยตรงอยู่แล้ว ไม่ต้องซ้ำ)
- ไฟล์ที่แก้: `web/src/pages/documents/DocumentDetail.tsx`, `web/src/components/MemoSheet.tsx`

**🔴 พบและแก้ไขข้อมูลลับหลุดในไฟล์โปรเจกต์: รหัสผ่านแอป Gmail จริงฝังอยู่ใน `server/.env.example`**
- เจอระหว่างเตรียมคำแนะนำ setup ให้: `server/.env.example` (และไฟล์ `server/.env` ที่หลงเหลืออยู่) มีอีเมล/รหัสผ่านแอป
  Gmail จริงของโรงเรียนฝังอยู่ตรงๆ (`SMTP_USER`, `SMTP_PASS`) รวมถึงมีอีเมลจริงถูก hardcode ไว้เป็นค่า fallback ใน
  โค้ด `server/scripts/backup.js` ด้วย — ทั้งหมดนี้อยู่ในไฟล์ตั้งแต่ต้นทาง (ไฟล์ที่อัปโหลดมาให้ตอนแรก) ไม่ได้เกิดจาก
  การแก้ไขระหว่างทำงานรอบนี้
- **สิ่งที่ต้องทำเพิ่มเติมด้วยตัวเอง (สำคัญมาก):** ไปที่ Google Account ของอีเมลนั้น → Security → App passwords
  → **เพิกถอน (revoke) รหัสผ่านแอปตัวนี้ทิ้งแล้วสร้างใหม่** เพราะรหัสผ่านที่หลุดมาอาจเคยถูกส่งต่อ/เก็บไว้ที่อื่นแล้ว
  การลบออกจากไฟล์อย่างเดียวไม่พอ ต้องเปลี่ยนรหัสผ่านจริงด้วย
- แก้แล้ว: ลบ `server/.env` ออกจากโปรเจกต์ที่ส่งกลับให้ทั้งหมด, แทนที่ค่าจริงใน `.env.example` ด้วย placeholder,
  ตัดอีเมล hardcode ออกจาก `backup.js` (เปลี่ยนเป็นข้ามการส่งอีเมลอย่างปลอดภัยถ้าไม่ได้ตั้งค่าไว้ — ยังคงรีสตาร์ท
  พอร์ตเว็บตามปกติทุกคืนเหมือนเดิม ไม่กระทบพฤติกรรมส่วนนี้), แทนที่อีเมลจริงในเอกสาร README ด้วย placeholder เช่นกัน

---

## v2.5.0 — 2026-09-17

**เปลี่ยนแนวทางฟิลด์ "เนื้อหาอิสระ" ของเอกสารที่รูปแบบไม่ตายตัว (เช่น คำสั่งโรงเรียน)**

หลังทดลองใช้ฟิลด์ "ข้อความอิสระ" (richtext, v2.4.0) จริง พบว่าการพยายามเลียนแบบ Word ทั้งหมดในเบราว์เซอร์
(วาง/ตัดข้อความจาก Word, จัดหน้า, แบ่งหน้าตอนพิมพ์, ลิสต์/ตาราง) มีปัญหาจุกจิกมากเกินไปและแก้ไม่จบ — จึงเปลี่ยน
แนวทางสำหรับเอกสารกลุ่มนี้ตามที่ตกลงกัน: **ให้แนบไฟล์ Word/PDF จริงแทนการพิมพ์ในระบบทั้งฉบับ**

- **ฟิลด์ชนิดใหม่ `fileContent`** — แนบไฟล์ .doc/.docx/.odt/.rtf/.pdf ให้เป็น "ตัวเอกสารจริง" (ตั้งค่าได้ที่หน้า
  "ประเภทเอกสาร" เลือกชนิดฟิลด์ "แนบไฟล์เอกสาร")
- **แปลง Word → PDF อัตโนมัติที่ฝั่งเซิร์ฟเวอร์** ด้วย LibreOffice headless (`server/lib/officeConvert.js`) — ทำ
  ให้หน้าตาเอกสารเหมือนกันทุกเครื่องที่เปิดดู ไม่ขึ้นกับฟอนต์ที่ติดตั้งในเครื่องผู้อ่านแต่ละคน (ทดสอบแปลงจากไฟล์
  ตัวอย่างจริงแล้ว อ่านออกครบ ไม่มีตัวอักษร/สระ/วรรณยุกต์เพี้ยน แม้เครื่องทดสอบจะไม่มีฟอนต์ตรงกับต้นฉบับเป๊ะก็ตาม)
- **เพิ่มโฟลเดอร์ `server/fonts/` + สคริปต์ติดตั้งฟอนต์** (`install-fonts.ps1` สำหรับ Windows, `install-fonts.sh`
  สำหรับ Linux/Mac) — ให้ใส่ฟอนต์ราชการไทย (เช่น TH Sarabun New) แล้วรันสคริปต์ติดตั้งระดับเครื่อง เพื่อให้ผลลัพธ์
  PDF ตรงกับต้นฉบับที่สุด เซิร์ฟเวอร์จะเตือนตอนสตาร์ทถ้ายังไม่พบฟอนต์ไทยในเครื่อง
- **แสดงตัวอย่าง PDF ฝังอยู่ในหน้าเว็บเลย** (`InlinePdfPreview.tsx`) ไม่ใช่แค่ลิงก์ดาวน์โหลด รองรับมือถือ/iPad/PC
  ปุ่ม "พิมพ์" ของเอกสารประเภทนี้จะสั่งพิมพ์จากตัวอย่าง PDF โดยตรง
- ส่วนการเซ็นรับทราบของฟอร์มกลุ่มนี้ ใช้ระบบ "ส่งต่องาน + กดรับทราบ" ที่มีอยู่แล้วในระบบตามปกติ (ไม่ต้องฝัง
  ลายเซ็น/รายชื่อไว้ในเนื้อหาไฟล์)
- ฟิลด์ "ข้อความอิสระ" (richtext) เดิมยังใช้งานได้ตามปกติสำหรับเอกสารที่พิมพ์สั้นๆ ในระบบสะดวกกว่าแนบไฟล์ (ยังคง
  bug fix เรื่องการวางจาก Word ของ v2.4.0 ไว้เหมือนเดิม) — เป็นทางเลือกคู่กัน ไม่ได้ตัดออก

ไฟล์ใหม่: `server/lib/officeConvert.js`, `server/fonts/README.md`, `server/scripts/install-fonts.ps1`,
`server/scripts/install-fonts.sh`, `web/src/components/InlinePdfPreview.tsx`, `web/src/components/FileContentPicker.tsx`
ไฟล์ที่แก้: `server/server.js` (เพิ่ม `processFileContentFields`, เช็คฟอนต์/LibreOffice ตอนสตาร์ท), `web/src/api/client.ts`,
`web/src/pages/admin/AdminDocumentTypes.tsx`, `web/src/pages/documents/DocumentCreate.tsx`,
`web/src/pages/documents/DocumentEdit.tsx`, `web/src/pages/documents/DocumentDetail.tsx` (พรีวิว PDF เด่นในหน้า +
ปุ่มพิมพ์ชี้ไปที่ PDF โดยตรงถ้าเป็นเอกสารประเภทนี้)

---

## v2.4.0 — 2026-09-16

งานรอบนี้ (ตามที่แจ้งมาหลังใช้ฟีเจอร์ "ข้อความอิสระ" จริง):

**ฟีเจอร์ "ข้อความอิสระ" (richtext) — ปรับปรุงให้ใกล้เคียง Word มากขึ้น**
- แก้การวาง (paste) จาก Word/Google Docs ที่เคยทำให้เกิดการเยื้องเพี้ยน (หน่วยนิ้วแปลงเป็น px และจำกัดเพดานแล้ว),
  blockquote ซ้อนกันลึกเกินไป (จำกัดไว้ไม่เกิน 3 ชั้น), และ `&nbsp;`/บรรทัดว่างซ้ำกันเป็นตับ (ยุบให้เหลือน้อยลงอัตโนมัติ)
- ตัวแก้ไขมีพื้นหลังขาวจำลองกระดาษ พร้อมขนาดตัวอักษร/ระยะบรรทัดเท่ากับตอนพิมพ์จริง และมีข้อความจาง
  "เริ่มพิมพ์เนื้อหาที่นี่..." ตอนว่าง ให้ความรู้สึกใกล้เคียงโปรแกรมประมวลผลคำมากขึ้น

**แก้หน้ารายละเอียดเอกสารโชว์ HTML tag ดิบ**
- ฟิลด์ richtext ในหน้าสรุปข้อมูลแบบย่อ (ก่อนกด "พิมพ์") เคยโชว์ `<p>`, `<b>` ฯลฯ เป็นข้อความดิบแทนที่จะ render จริง — แก้แล้ว

**เพิ่มตัวเลือก "ซ่อนหัวเอกสารมาตรฐาน" ต่อประเภทเอกสาร**
- ใช้เมื่อฟิลด์ข้อความอิสระพิมพ์หัวจดหมาย (ชื่อโรงเรียน/ที่-วันที่/เรื่อง/เรียน) เองอยู่แล้วในเนื้อหา จะได้ไม่ขึ้นซ้ำสองที่
- ตั้งค่าได้ที่หน้า "ประเภทเอกสาร" ของแต่ละประเภท

**แก้ปัญหาพิมพ์เอกสารยาวเกิน 1 หน้า A4 แล้วเนื้อหาส่วนเกินถูกตัดหายไปเงียบๆ**
- เดิมล็อกความสูงหน้ากระดาษไว้ตายตัว + `overflow: hidden` เพื่อบังคับพิมพ์ 1 หน้าเสมอ (กันบล็อกลงชื่อที่แต่ก่อนใช้
  `position: absolute` เกาะมุมกระดาษไปโผล่หน้าที่ 2) ผลคือเนื้อหาที่ยาวเกิน 1 หน้าถูกตัดทิ้งจากที่พิมพ์จริงโดยไม่มีการเตือน
- เปลี่ยนบล็อกลงชื่อให้ไหลตามเนื้อหาปกติ (เหมือน Word/PDF) แทน และใช้ `@page margin` ให้ขอบกระดาษซ้ำถูกต้องทุกหน้า
  ตอนนี้เอกสารยาวหลายหน้าพิมพ์ออกมาครบทุกหน้า ไม่มีเนื้อหาหายอีกต่อไป

**🔴 แก้บั๊กใหญ่: server รีสตาร์ท (deploy ระบบใหม่/เครื่องรีบูต/ล่มแล้วเด้งเอง) แล้วบังคับ logout ทุกคนพร้อมกัน**
- สาเหตุจริง: session เดิมเก็บอยู่ใน memory ล้วนๆ (`Map` ในโปรเซส Node) พอโปรเซสรีสตาร์ท ข้อมูล session หายทันที
  ทั้งที่ cookie ในเบราว์เซอร์ผู้ใช้ยังไม่หมดอายุเลย ทำให้ทุกคนโดนบังคับออกจากระบบพร้อมกันโดยไม่รู้ตัว
  ไม่รู้ว่าหนังสือเข้ามาใหม่เมื่อไหร่จนกว่าจะ login เองอีกครั้ง
- ย้าย session ไปเก็บใน SQLite (ตาราง `sessions`) แทน — อยู่รอดข้ามการรีสตาร์ทได้แล้ว ทดสอบแล้วว่า login ทิ้งไว้ →
  รีสตาร์ท server จำลองการ deploy จริง → ยัง login ค้างอยู่ ไม่ต้อง login ใหม่
- ไฟล์ที่แก้: `server/lib/auth.js` (เขียนใหม่ทั้งไฟล์), `server/db/sqliteStore.js` (เพิ่ม collection `sessions`)

**Push notification หยุดทำงานเองหลังผ่านไปสักพัก**
- สาเหตุ: ไม่มีระบบตรวจสอบ/ต่ออายุ subscription เลย พอเบราว์เซอร์หมุน endpoint ใหม่เอง (พบได้ในบาง browser ตามรอบเวลา)
  หรือ service worker ถูกอัปเดต subscription เดิมจะใช้ไม่ได้อีกต่อไปเงียบๆ โดยไม่มีใครรู้ตัว
- เพิ่ม `pushsubscriptionchange` listener ใน service worker (ดักจับตอนเบราว์เซอร์หมุน endpoint ใหม่แล้ว subscribe ซ้ำอัตโนมัติ)
- เพิ่ม `syncPushSubscription()` เรียกตอนเปิดแอป/กลับมา foreground ทุกครั้ง เช็คสุขภาพ subscription แล้วซ่อมให้อัตโนมัติแบบเงียบๆ
  ถ้าเคยกดเปิดแจ้งเตือนไว้แล้วแต่ subscription หายไป จะ subscribe ใหม่ให้เองโดยไม่ต้องเข้าไปกดปุ่มซ้ำ
- ไฟล์ที่แก้: `web/public/sw.js`, `web/src/lib/push.ts`, `web/src/main.tsx`

**จัดระเบียบเอกสารในโปรเจกต์**
- รวมไฟล์ .md ที่กระจัดกระจาย (อ่านก่อน.md, START_HERE.md, CHANGELOG_THIS_ROUND.md, CHANGES_FROM_REVIEW.md)
  เข้ามาไว้ในไฟล์เดียวนี้และ README.md เหลือไฟล์หลักแค่ README.md (เริ่มต้น/ภาพรวม) + CHANGELOG.md (ประวัติ) ที่ root
  ส่วน server/README.md, web/README.md, server/static-forms/README.md ยังคงแยกไว้ตามเดิม เพราะเจาะจงเฉพาะโฟลเดอร์นั้นๆ
- ปรับเลขเวอร์ชันจาก 2.3.0 → 2.4.0 (ตรงกันทั้ง `web/package.json`, `server/package.json`, `web/src/version.ts`,
  `server/version.js` — ดูได้ที่หน้า "สถานะระบบ & Log" มุมขวาเลขเวอร์ชัน ถ้ายังขึ้น v เก่ากว่านี้แปลว่ายังไม่ได้ build/deploy
  โค้ดชุดล่าสุด)

ไฟล์ที่แก้ทั้งหมดรอบนี้: `web/src/utils/richText.ts`, `web/src/components/RichTextEditor.tsx`,
`web/src/components/MemoSheet.tsx`, `web/src/pages/documents/DocumentDetail.tsx`, `web/src/pages/admin/AdminDocumentTypes.tsx`,
`web/src/api/client.ts`, `web/src/index.css`, `server/server.js`, `server/lib/auth.js`, `server/db/sqliteStore.js`,
`web/public/sw.js`, `web/src/lib/push.ts`, `web/src/main.tsx`, เลขเวอร์ชันทั้ง 4 ไฟล์

---

## เนื้อหาที่รวมมาจาก CHANGELOG.md (ประวัติเดิม)


---

## v2.3.0 — 2026-08-06

### 1. บั๊กสำคัญที่สุด: หัวหน้าแผนก (เช่นหัวหน้าการเงิน) เปิดดูเอกสารที่ส่งมาแล้วไม่ขึ้น
- สาเหตุ: ระบบมีจุดตรวจสิทธิ์ **ไม่ตรงกันเอง** ระหว่างจุดต่างๆ — ตอนแสดงรายการงาน/นับแจ้งเตือน อนุญาตให้
  "สมาชิกทุกคนในแผนก" เห็นงานที่ส่งถึงแผนกได้ แต่ตอนเปิดดูเอกสารจริง (`canView`) กลับอนุญาตเฉพาะ
  **"หัวหน้าแผนกเท่านั้น"** ถ้าการตั้งค่าหัวหน้าแผนก (ที่ระเบียนแผนก) กับสมาชิกแผนก (ที่ระเบียนผู้ใช้) ไม่ตรง
  กันเป๊ะ (คนละจุดข้อมูลกัน) จะเจอปัญหาแบบ "เห็นในรายการ แต่กดเข้าไปแล้วว่างเปล่า" ทันที
- **แก้แล้ว**: ปรับให้ `canView` และจุดอื่นที่เกี่ยวข้อง (รับทราบงาน, ปิดงาน, นับแจ้งเตือน — รวม 4 จุด) ยึด
  กติกาเดียวกันทั้งหมด และเผื่อไว้สองชั้น (เช็คทั้งจากรายชื่อสมาชิกแผนกของผู้ใช้ และเช็คตรงจากผู้ที่ถูกตั้งเป็น
  หัวหน้าแผนก) กันกรณีข้อมูลสองจุดไม่ตรงกันแบบที่เจอ

### 2. ระบบเส้นทางอนุมัติแบบกำหนดเอง — ส่งตรงถึงหัวหน้าแผนก ข้ามหัวหน้าสำนักงาน/ผอ. ได้
- ที่หน้า "ประเภทเอกสาร" ตั้งค่า "เส้นทางอนุมัติ" ได้ 2 แบบ: **ปกติ** (ผ่านหัวหน้าสำนักงานตรวจกรอง แล้วส่งต่อ
  ผอ. เกษียนตามเดิม) หรือ **ส่งตรงถึงหัวหน้าแผนก** (ข้ามหัวหน้าสำนักงานและ ผอ. ไปเลย) — เลือกได้ว่าจะล็อก
  แผนกปลายทางตายตัวไว้ในประเภทเอกสารเลย หรือให้ผู้จัดทำเอกสารเลือกแผนกเองตอนสร้างเอกสาร (มีระบบเตือนถ้า
  แผนกที่เลือกยังไม่มีหัวหน้าแผนกกำหนดไว้ กันปัญหาแบบข้อ 1)
- ใช้กับเอกสารที่ผู้จัดทำส่งตรงถึงหัวหน้าแผนกอื่น เช่น หัวหน้าการเงิน งานทะเบียน งานพัสดุ โดยไม่ต้องผ่านการ
  ตรวจกรอง/เกษียนตามขั้นตอนปกติ

### 3. หน้าแดชบอร์ดของ ผอ. — ตัดข้อมูลที่ยังไม่เกี่ยวกับท่านออก + เพิ่มข้อมูล "งานค้างอยู่ตรงไหน"
- ตัดขั้นตอน "รอตรวจกรอง" (ยังไม่ถึงคิว ผอ.) ออกจากสรุปสถานะที่ ผอ. เห็น เหลือเฉพาะขั้นตอนที่เกี่ยวกับท่าน
  โดยตรงหรือหลังจากนั้น ลดความสับสนว่า "ทำไมมีงานค้างเยอะ" ทั้งที่ยังไม่ถึงคิวท่านเลย
- เพิ่มการ์ดใหม่ **"เอกสารที่ค้างระหว่างดำเนินการนานที่สุด"** (เห็นได้ทั้ง ผอ./หัวหน้าสำนักงาน/แอดมิน) แสดงว่า
  แต่ละเรื่องรอใคร/แผนกไหนอยู่ ค้างมากี่วันแล้ว กดเข้าไปอ่านประวัติ/log รายละเอียดได้ทันที ไม่ต้องไล่เปิดทีละ
  เอกสารเพื่อหาว่าอันไหนติดอยู่ตรงไหน

### 4. แก้ไขเอกสารได้ระหว่างที่ยังอยู่สถานะ "รอตรวจกรอง"
- เดิมแก้ไขได้เฉพาะตอนถูกตีกลับเท่านั้น ตอนนี้ผู้จัดทำเอกสารแก้ไขข้อมูล/ไฟล์แนบได้ตลอดตราบใดที่ยังไม่มีใคร
  เริ่มพิจารณา (สถานะยังเป็น "รอตรวจกรอง" อยู่) โดยไม่ต้องเปลี่ยนสถานะ/เข้าคิวใหม่ พอเริ่มมีการพิจารณาแล้ว
  จะแก้ไม่ได้อีกจนกว่าจะถูกตีกลับกลับมา (ใช้หน้าแก้ไขเดียวกับตอนตีกลับ ข้อความ/ปุ่มปรับตามสถานะให้เหมาะสม)

### 5. ระบบเก็บฟอร์ม PDF แบบคงที่ (สำหรับฟอร์มที่ยังไม่รองรับกรอกดิจิทัล)
- เพิ่มโฟลเดอร์ `server/static-forms/` (มี README อธิบายวิธีตั้งชื่อไฟล์) — ที่หน้า "ประเภทเอกสาร" ตั้งค่า
  "ชนิดฟอร์ม" เป็น "ไฟล์ PDF คงที่" แล้วระบุชื่อไฟล์ที่ตรงกับไฟล์ในโฟลเดอร์นั้นได้
- พอผู้ใช้งานเลือกประเภทเอกสารแบบนี้ตอนสร้างเอกสาร ระบบจะไม่แสดงฟอร์มดิจิทัล แต่แสดงข้อความแจ้งว่ายังไม่
  รองรับกรอกดิจิทัล พร้อมพรีวิวไฟล์ PDF ในหน้า และปุ่มดาวน์โหลด/สั่งพิมพ์ (ไม่มีการบันทึกเป็นเอกสารในระบบ
  เพราะเป็นแค่การดาวน์โหลด/พิมพ์ฟอร์มไปกรอกด้วยมือ)

### 6. กู้คืนข้อมูลสำรองแล้วเว็บล่มเป็นเวลานาน
- สาเหตุจริง 2 จุดซ้อนกัน: (1) โค้ดเดิมเขียน log การกู้คืนไปที่ฐานข้อมูลที่เพิ่งปิดและสลับไฟล์ไปแล้ว ทำให้ error
  "database connection is not open" ทุกครั้งที่กู้คืน ทั้งที่ไฟล์จริงกู้คืนสำเร็จแล้ว (2) การรีสตาร์ทระบบเดิม
  พึ่งพา NSSM (มีเฉพาะบนเครื่อง production) — บนเครื่อง dev ที่รันตรงๆ ไม่มีตัวรีสตาร์ทอัตโนมัติเลย ทำให้
  เซิร์ฟเวอร์ตายค้างจนกว่าจะรันเองใหม่
- **แก้แล้วทั้งคู่**: เปลี่ยนการบันทึก log การกู้คืนไปเป็นไฟล์ text แยกต่างหาก (`backups/restore-history.log`)
  แทนการเขียนลง DB ที่กำลังจะถูกแทนที่ และเปลี่ยนมาเปิด-ปิด database connection สดในโปรเซสเดียวกันแทนการ
  restart ทั้งแอป (ใช้งานได้เหมือนกันทั้ง dev และ production ไม่ต้องพึ่ง NSSM/PM2 อีกต่อไปสำหรับ flow นี้)

### 7. บั๊กยิบย่อยที่เจอระหว่างทดสอบ
- **ลบประเภทเอกสารไม่ได้**: ทำงานจริง (soft-delete) แต่หน้า Admin ไม่กรองออกจากรายการเลยดูเหมือนไม่มีอะไร
  เกิดขึ้น — แก้ให้กรองออกเป็นค่าเริ่มต้น พร้อมตัวกรอง "แสดงที่ปิดใช้งานแล้ว" และปุ่มกู้คืนกลับ
- **พิมพ์เอกสารแล้วมี 2 หน้า หน้า 2 ว่างเปล่ามีลายเซ็นลอยอยู่ด้านล่าง**: บั๊ก CSS คลาสสิก (`min-height` +
  ลูก `position:absolute; bottom:...` ล้นไปหน้า 2 ตอน render สูงเกิน 297mm ไปเสี้ยวมิลลิเมตร) — ล็อกความสูง
  พอดี 1 หน้าตอนพิมพ์ พร้อมเพิ่มการเตือนก่อนพิมพ์ถ้าเนื้อหายาวเกิน 1 หน้าจริงๆ (กันข้อมูลถูกตัดหายเงียบๆ)
- **พรีวิวเอกสารมุมโค้งมนไม่เท่ากันซ้าย-ขวา**: เกิดจาก scale ตายตัว (0.5) ไม่พอดีกับความกว้างจริงของกล่อง —
  เปลี่ยนเป็นคำนวณ scale อัตโนมัติตามความกว้างจริงบนจอ (ช่วยลดปัญหามือถือต้องซูมในหน้านี้ไปด้วย)
- **สถานะดิสก์ไม่ขึ้นบนเครื่อง server (Windows) ทั้งที่ dev local ขึ้นปกติ**: เพิ่ม fallback ด้วยคำสั่ง `wmic`
  สำหรับ Windows กรณี `fs.statfsSync` ใช้ไม่ได้ในบาง service account/environment

---

## v2.2.0 — 2026-08-04

### 1. แก้บั๊ก: เมนู 3 รายการค้างอยู่ภาษาเดียวไม่ว่าจะสลับภาษาไหน
- สาเหตุ: คีย์คำแปล `navManageDuty`, `navTranslations`, `navSystemLog` และ `navOpsGroupTitle` (หัวข้อกลุ่มเมนู)
  **ไม่เคยถูกใส่ไว้ในพจนานุกรมทั้ง 3 ภาษาเลยตั้งแต่แรก** ระบบเลย fallback ไปแสดง key ดิบๆ แทนคำแปล
  (ไม่ใช่บั๊กเฉพาะภาษาใดภาษาหนึ่ง — ทุกภาษาโดนหมด แค่บังเอิญคนละคำที่มองเห็นชัด)
- **แก้แล้ว**: เพิ่มคำแปลครบทั้งไทย/อังกฤษ/จีน แล้วตรวจสอบด้วยสคริปต์เทียบจำนวนคีย์ทุกไฟล์ในระบบว่า
  **ครบ 100% ทั้ง 373 คีย์ ทั้ง 3 ภาษาตรงกันหมดแล้ว** ไม่มีจุดขาดอื่นหลงเหลือ (ปิดงานค้างข้อ "แปลภาษาให้ครบ 100%"
  จากรอบก่อนไปด้วยในตัว)

### 2. เพิ่มประเภทเอกสารตามฟอร์ม PDF ที่ส่งมา ครบทั้ง 12 แบบ + ฟิลด์ตารางแบบใหม่
- ตรวจฟอร์ม PDF ที่แนบมา (ไฟล์สแกน 12 หน้า) พบฟอร์มที่ใช้จริงในโรงเรียน 12 แบบ — **เลือกทำเป็นฟอร์มดิจิทัล
  แบบมีโครงสร้าง (กรอกช่อง + พรีวิวเรียลไทม์) แทนแนวทาง PDF-overlay** ที่เคยตั้งใจไว้ในรอบก่อน เพราะกรอกง่ายกว่า
  บนมือถือ ตรวจสอบข้อมูลก่อนส่งได้ และไม่ผูกกับขนาด/ตำแหน่งกระดาษต้นฉบับตายตัว
- เพิ่มประเภทเอกสารใหม่ 10 แบบใน "ประเภทเอกสาร": แบบขอใช้รถยนต์, ใบตรวจรับวัสดุ, ใบขอจัดซื้อ/จัดจ้าง,
  แบบฟอร์มบันทึกการเปลี่ยนเวร, ใบเบิกเงินสวัสดิการการศึกษาบุตร (กท.16 ช.), ใบเบิกเงินสวัสดิการค่ารักษาพยาบาล
  (กท.4 ช.), แบบขอหนังสือรับรอง/แบบคำขอต่างๆ, ใบจัดสอนแทนแนบท้ายใบลา, ใบขอเบิกวัสดุ, แบบขออนุญาตดำเนินการ
  โครงการ/งาน
- เสริมฟอร์ม **"ใบลา" ที่มีอยู่เดิม** ให้ครบขึ้น (เพิ่ม: สอนวิชา, ระดับชั้น, ที่พักระหว่างลา, เบอร์โทร,
  สถิติลาป่วย/ลากิจสะสม) แทนที่จะสร้างซ้ำเป็นประเภทใหม่ — ข้อมูลเอกสารเก่าที่เคยสร้างไว้ก่อนหน้ายังใช้ได้ปกติ
  ไม่กระทบ ("บันทึกข้อความ" ทั่วไปก็มีอยู่แล้วในระบบ จึงไม่ได้สร้างซ้ำเช่นกัน)
- **เพิ่มชนิดฟิลด์ใหม่ "ตารางรายการ"** (`type: 'table'`) รองรับฟอร์มที่มีตารางกรอกได้หลายแถว เช่น รายการวัสดุ,
  รายการจัดซื้อ, ตารางจัดสอนแทน — ผู้กรอกกดเพิ่ม/ลบแถวได้เองตอนกรอกจริง ไม่ต้องจำกัดจำนวนแถวไว้ล่วงหน้า
  (ตั้งค่าคอลัมน์ได้จากหน้า "ประเภทเอกสาร" → เลือกชนิดฟิลด์ "ตารางรายการ")

### 3. พรีวิวเอกสารแบบเรียลไทม์ตอนกรอกฟอร์ม
- หน้า "สร้างเอกสาร" มีแผงพรีวิวขนาดย่ออยู่ข้างๆ ฟอร์ม (หรือใต้ฟอร์มบนมือถือ) แสดงหน้าตาเอกสารที่จะได้
  **อัปเดตทันทีตามที่พิมพ์** — ใช้ตัวเรนเดอร์เดียวกับที่พิมพ์จริง (`MemoSheet.tsx` แยกออกมาเป็นไฟล์กลาง)
  จึงรับประกันว่าสิ่งที่เห็นตอนกรอกตรงกับที่จะพิมพ์ออกมาจริงเป๊ะๆ ไม่ต้องดูแลสองที่แยกกันให้ตรงกันเอง

### 4. หน้าแก้ไขเอกสารที่ถูกตีกลับ แบบเต็มรูปแบบ (ของเดิมมีแค่ "ส่งใหม่" เฉยๆ)
- เพิ่มหน้าใหม่ `/documents/:id/edit` — ใช้ชุดฟิลด์เดียวกับหน้าสร้างเอกสารทุกอย่าง (รวมพรีวิวเรียลไทม์)
  ดึงข้อมูลเดิมมาเติมให้ครบทุกช่อง แก้ไขได้จริง ไม่ใช่แค่พิมพ์หมายเหตุแล้วส่งซ้ำแบบเดิม
- **ลบไฟล์แนบเดิมได้** และ**แนบไฟล์ใหม่/แทนที่ได้** (เพิ่ม endpoint `DELETE /api/documents/:id/attachments/:attId`
  — จำกัดให้ลบได้เฉพาะตอนเอกสารอยู่ในสถานะ "ถูกตีกลับ" เท่านั้น กันไม่ให้ลบไฟล์แนบของเอกสารที่อนุมัติ/กำลัง
  พิจารณาอยู่)
- แก้แล้วส่งใหม่ ระบบ**ลง log ว่าใครแก้ไข** (ชื่อ + เวลา) แต่**ไม่ลงรายละเอียดว่าแก้ตรงไหนบ้าง** ตามที่ขอ
  (เหมือนเดิม แต่ตอนนี้ข้อมูลที่แก้จริงถูกบันทึกครบถ้วนกว่าเดิม ไม่ใช่แค่ subject/fields)

### 5. กู้คืนเอกสารจากถังขยะได้ภายใน 30 วัน โดยผู้ที่ตีกลับ/ไม่อนุมัติเอกสารนั้นเอง
- เดิมเอกสารที่ถูกไม่อนุมัติ (เข้าถังขยะ) กู้คืนไม่ได้เลย ต้องให้ผู้จัดทำสร้างใหม่ทั้งหมด
- **แก้แล้ว**: ตอนตีกลับ/ไม่อนุมัติ ระบบบันทึกว่า**ใครเป็นคนตีกลับ**และ**เมื่อไหร่** เพิ่ม action ใหม่
  `restore` — กู้คืนได้เฉพาะคนที่ตีกลับเอกสารนั้นเอง (หรือแอดมิน) และต้องกู้คืน**ภายใน 30 วัน**นับจากวันที่
  ไม่อนุมัติเท่านั้น พ้นกำหนดแล้วกู้คืนไม่ได้อีก กู้คืนแล้วเอกสารกลับไปอยู่ในขั้นตอนตรวจสอบที่ค้างอยู่ ณ
  ตอนที่ถูกตีกลับ (ไม่ต้องเริ่มขั้นตอนใหม่ทั้งหมด)
- หน้ารายละเอียดเอกสารที่อยู่ในถังขยะ แสดงว่าใครย้ายเข้าถังขยะ + เหลือเวลากู้คืนอีกกี่วัน

### 6. บั๊กมือถือ: กดเข้าแอพผ่าน push notification แล้วโหลดไม่ขึ้น/ค้าง (แจ้งเพิ่มระหว่างทาง)
- พบสาเหตุ 3 จุดร่วมกัน:
  1. **Service worker** (`sw.js`): เดิมกดแจ้งเตือนแล้วเปิดหน้าต่างใหม่ทุกครั้ง (`clients.openWindow()`)
     แม้แอพจะเปิดอยู่แล้วในแท็บเดิม ทำให้ต้อง**บูตแอพทั้งชุดซ้ำสอง** — แก้ให้เช็คแท็บที่เปิดอยู่ก่อน
     ถ้าเจอให้ navigate ไปหน้าที่ต้องการแล้วโฟกัสแท็บเดิมแทน เปิดหน้าต่างใหม่เฉพาะตอนไม่มีแท็บเปิดอยู่จริงๆ
  2. **ไม่มี timeout ตอนเรียก API เลยสักที่เดียว**: ถ้ามือถือเพิ่งตื่นจาก background (เช่นกดแจ้งเตือน)
     ตอนที่สัญญาณเน็ต/ไวไฟยังไม่กลับมาเต็มที่ คำขออาจค้างเงียบๆ ได้นานมากโดยไม่มี error ใดๆ ขึ้นเลย
     ทำให้แอพดูเหมือน "โหลดไม่ขึ้น" ทั้งที่จริงกำลังรอ — เพิ่ม timeout 15 วินาทีให้ทุกคำขอ พร้อมข้อความ
     แจ้งเตือนที่เข้าใจง่ายเมื่อหมดเวลา
  3. เพิ่มการเช็คสถานะล็อกอินใหม่อัตโนมัติทุกครั้งที่แอพกลับมาอยู่หน้าจอ (`visibilitychange`/`pageshow`)
     แทนที่จะค้างสถานะเดิมที่อาจหลุดไปแล้วเงียบๆ ระหว่างอยู่ background
  4. เพิ่มหน้าจอโหลด (splash) ตอนบูตแอพครั้งแรก — เดิมจอขาวเปล่าจนกว่า JS จะรันเสร็จ ดูเหมือนค้างทั้งที่
     กำลังโหลดอยู่จริง

### 7. เลขเวอร์ชันระบบ
- อัปเดต `server/version.js`, `web/src/version.ts`, `web/package.json` เป็น `2.2.0` ตามรอบแก้ไขนี้

---



## v2.1.0 — 2026-08-03

### 1. ระบบเวรครู — บันทึกร่างได้ตลอดวัน + ส่งครั้งเดียวหลังเวลาที่กำหนด
- เดิมต้องบันทึกแยกทุกช่วงเวลาใน 1 วัน (เช้า/เที่ยง/เย็น) — เปลี่ยนเป็น **1 การ์ดต่อ 1 เวรต่อวัน**
- เพิ่มปุ่ม **"บันทึกร่างไว้ก่อน"** (`PUT /api/duty/logs/draft`) — กรอก/แนบรูปสะสมได้ตลอดวัน ไม่บังคับกรอกครบ
  ไม่เช็คเวลา และ **autosave แบบ debounce** (พิมพ์แล้วรอ ~1.2 วิ ระบบบันทึกร่างให้อัตโนมัติ)
- ร่างที่บันทึกไว้จะไม่หายแม้ logout แล้ว login ใหม่ (ดึงจาก server ไม่ใช่แค่ localStorage)
- ปุ่ม **"ส่งเวร"** (`POST /api/duty/logs/submit`) กดได้ครั้งเดียวต่อวัน และเปิดใช้งานได้เฉพาะ**หลังเวลา 17:20**
  เป็นต้นไปเท่านั้น (ปรับได้ผ่าน env `DUTY_SUBMIT_CUTOFF` ที่ `server/.env`)
- หน้าเวรของหัวหน้าเวร (`GET /api/duty/oversight`) เพิ่มรายการ **"ยังไม่ได้บันทึกเวรวันนี้"** แยกให้เห็นชัดเจน
  (เดิมแสดงเฉพาะรายการที่ทำแล้วเท่านั้น มองไม่เห็นว่าใครยังไม่ส่ง)
- หลังหัวหน้าเวรตรวจ (เซ็นรับทราบ) แล้ว **ล็อกแก้ไขไม่ได้อีก** แต่ยังเปิดดูรายละเอียดย้อนหลังได้เสมอ
  (ถ้ายังไม่มีการตรวจ ครูเวรยังแก้ไขร่าง/ที่ส่งไปแล้วได้ตามปกติ)
- ผังการจัดเวร (ใครเวรวันไหน/เวลาไหน) **ไม่เปลี่ยนแปลง** ยังใช้ของเดิมได้ปกติ — เปลี่ยนเฉพาะฝั่ง "บันทึกผล" เท่านั้น

### 2. แปลภาษาให้ครบ 3 ภาษา (th/en/zh)
- พบว่าหน้า **"สถานะระบบ/Log"** มีข้อความไทย hardcode ~10 จุด ไม่ใช้ระบบแปลภาษาเลย — แก้ให้ใช้ `t()` ครบแล้ว
- หน้า "จัดการเวรครู" และ "จัดการคำแปล" ตรวจสอบแล้วใช้ระบบแปลภาษาครบถ้วนอยู่แล้ว ไม่ต้องแก้เพิ่ม
- เพิ่มคีย์คำแปลใหม่รวม ~40 คีย์ ครบทั้ง 3 ภาษา (ตรวจสอบด้วยสคริปต์เทียบจำนวนคีย์ทุกภาษาแล้วว่าตรงกัน 100%)

### 3. หน้า "สถานะระบบ/Log" — เพิ่มการตรวจพื้นที่ดิสก์ของเครื่อง server
- เพิ่มแถบแสดงพื้นที่ดิสก์ที่ใช้ไป/เหลือของ**เครื่องที่รันเซิร์ฟเวอร์อยู่จริง** (ไม่ใช่แค่ NAS เหมือนเดิม)
- ใช้ `fs.statfsSync()` ตรวจจากโฟลเดอร์ที่แอปรันอยู่จริงโดยตรง — **ไม่ผูกกับ path ตายตัว** (เช่น `C:\`) จึงใช้ได้ถูกต้อง
  ทั้งตอน dev (เครื่องพัฒนา) และตอนรันจริงบนเครื่อง server โดยไม่ต้องแก้โค้ดสลับกันเอง
- ขึ้นเตือนสีแดงอัตโนมัติเมื่อพื้นที่ใช้ไปเกิน 85%

### 4. เก็บไฟล์สำรอง (backup) 1 ปี แทน 60 ชุด
- เปลี่ยนจากนับจำนวนไฟล์ (`BACKUP_KEEP=60`) เป็นนับอายุจริง (`BACKUP_KEEP_DAYS=365`)
- ปรับ `.env`, `.env.example`, README ให้ตรงกัน

### 5. แก้พื้นที่ดิสก์บวมจากการสำรองข้อมูล (ปัญหาที่แจ้งเพิ่มระหว่างทาง)
- เดิมทุกคืนคัดลอกไฟล์ในโฟลเดอร์ `uploads/` **ซ้ำทุกไบต์ทุกชุด** — มี 10 ไฟล์วันนี้ พรุ่งนี้ backup ใหม่ก็คัดลอกซ้ำ
  10 ไฟล์เดิมอีก พื้นที่ดิสก์จึงเพิ่มแบบทวีคูณตามจำนวนวันที่เก็บ (ยิ่งเก็บนานเป็นปีตามข้อ 4 ยิ่งกินพื้นที่มาก)
- **แก้แล้ว**: ไฟล์ใน `uploads/` (ไฟล์แนบ/รูปเวร ซึ่งเขียนครั้งเดียวไม่แก้ไขซ้ำ) เปลี่ยนไปใช้ **hard link**
  แทนการคัดลอกไบต์จริง — แทบไม่เปลืองพื้นที่เพิ่มต่อไฟล์ที่ไม่เปลี่ยน ถ้า link ไม่ได้ (เช่นคนละไดรฟ์) จะ
  fallback ไปคัดลอกจริงอัตโนมัติ ไม่ทำให้ backup ล้มเหลว (ดูรายละเอียดที่ `server/README.md` ข้อ 3.2)
- โฟลเดอร์ `data/` (ฐานข้อมูล) ยังคัดลอกไบต์จริงเหมือนเดิม เพราะเปลี่ยนแปลงบ่อย ต้องได้สแนปช็อตจริง ณ เวลานั้น

### 6. แก้ "กู้คืนข้อมูลล้มเหลว: EPERM" บน Windows (ปัญหาที่แจ้งเพิ่มระหว่างทาง)
- สาเหตุ: better-sqlite3 ถือ handle ไฟล์ `database.db` เปิดค้างตลอดอายุโปรเซส บน Windows จะลบ/ทับโฟลเดอร์
  ที่มีไฟล์เปิดค้างไม่ได้ (`EPERM`)
- **แก้แล้ว**: ปิดการเชื่อมต่อฐานข้อมูลก่อนแตะโฟลเดอร์ `data/` ทุกครั้งที่กู้คืน แล้ว **รีสตาร์ทเซิร์ฟเวอร์ให้
  อัตโนมัติ** ทันทีหลังกู้คืนสำเร็จ (เหมือน pattern รีสตาร์ทตอนตี 3 ที่มีอยู่แล้ว) — ไม่ต้องเข้าไปรีสตาร์ทเอง
  ที่เครื่อง server อีกต่อไป (ต้องมี NSSM/PM2 ตั้ง auto-restart ไว้ตามที่ README แนะนำอยู่แล้ว)

### 7. ประเภทเอกสาร — เพิ่มชนิดฟิลด์ให้หลากหลายขึ้น
- ระบบสร้างฟอร์ม (ที่มีอยู่แล้ว แบบเรียงลำดับฟิลด์ ไม่ใช่ drag-and-drop แบบเก่าที่เคยมีบั๊ก) เพิ่มชนิดฟิลด์ใหม่:
  **ตัวเลข, เวลา, ตัวเลือกแบบปุ่มกลม (radio), checkbox เดี่ยว** (เดิมมีแค่ ข้อความ/ข้อความยาว/วันที่/dropdown)
- แนวทาง PDF-overlay (แนบ PDF ฟอร์มเดิม + วางกล่องกรอกทับ) ยังไม่ได้ทำในรอบนี้ — รอไฟล์ฟอร์มที่จะสแกนส่งมา
  เพิ่มเติมก่อน เพื่อประเมินว่าฟอร์มไหนเหมาะกับแนวทางนี้เป็นพิเศษ

### 8. มือถือ: แก้ปัญหาเลือกประเภทเอกสารแล้วมองไม่เห็นฟอร์ม
- เดิมกดเลือกประเภทเอกสารแล้วฟอร์มโผล่ด้านล่าง แต่จอไม่เลื่อนตาม บนมือถือจอสั้นจึงดูเหมือนไม่มีอะไรเกิดขึ้น
- **แก้แล้ว**: กดเลือกแล้วจอ**เลื่อนไปหาฟอร์มให้อัตโนมัติ** + ยุบรายการปุ่มเลือกประเภทเหลือแค่ **"ชิป" ของ
  ประเภทที่เลือกไว้ + ปุ่มเปลี่ยน** ทำให้ฟอร์มอยู่ใกล้สายตากว่าเดิมมาก ไม่ต้องเลื่อนผ่านปุ่มอื่นอีก

### 9. เลขเวอร์ชันระบบ
- เพิ่ม `server/version.js` และ `web/src/version.ts` เป็นจุดอ้างอิงเวอร์ชันเดียว แสดงที่หน้า "สถานะระบบ/Log"
- ตั้งใจให้ **เพิ่มเลขเวอร์ชันทุกครั้งที่แก้ไขระบบ** คู่กับการอัปเดตไฟล์นี้

---

## งานที่ยังไม่ได้ทำในรอบนี้ (ต่อได้เมื่อพร้อม)
- (ว่าง — งานค้างจากรอบก่อนหน้าทำครบแล้วในรอบ v2.2.0/v2.3.0)
- Responsive มือถือกว้างๆ ทั้งแอป — รอบนี้แก้เฉพาะจุดที่เจอสาเหตุชัดเจน (พรีวิวเอกสารในหน้าสร้าง/แก้ไข)
  ส่วนหน้าอื่นๆ ยังไม่ได้ตรวจทุกหน้า เพราะไม่มีอุปกรณ์มือถือจริงให้ทดสอบ — ถ้าเจอหน้าไหนมีปัญหา แจ้งชื่อหน้า/
  สิ่งที่ทำมาด้วยจะช่วยได้ตรงจุดกว่า
- ฟอร์มราชการที่ซับซ้อนมาก 2 แบบ (กท.16 ช. และ กท.4 ช.) ตอนนี้ทำเป็นฟอร์มดิจิทัลแบบครอบคลุมประเด็นหลัก
  ยังไม่ได้ทำเวอร์ชันที่ตรงกับแบบฟอร์มราชการต้นฉบับทุกช่องย่อย 100% — ถ้าต้องใช้ยื่นหน่วยงานราชการภายนอก
  โดยตรง แนะนำตรวจทานช่องให้ครบก่อนใช้งานจริง หรือใช้ระบบฟอร์ม PDF คงที่ใหม่ (ข้อ 5 ของ v2.3.0) แทนถ้า
  ต้องการความถูกต้องตรงต้นฉบับ 100%

---

## เนื้อหาที่รวมมาจาก CHANGELOG_THIS_ROUND.md

# สรุปการแก้ไขรอบนี้ (รอบล่าสุด)

## บั๊กที่แก้ไข (เจอสาเหตุแล้ว)

1. **แผนก: 1 คนอยู่ได้แค่แผนกเดียว** — โมเดลข้อมูลรองรับหลายแผนกอยู่แล้ว (departmentIds เป็น array)
   แต่ไม่เคยมี UI ให้ตั้งค่าเลย แก้แล้ว: รวมหน้า "ผู้ใช้งาน" และ "แผนก" เป็นหน้าเดียว มีช่องค้นหา
   ชื่อ (จำเป็นเมื่อบุคลากรมีหลักร้อยคน) + ชิปกรองตามแผนก (โชว์จำนวนสมาชิก) +ติ๊กเลือกได้หลายแผนก
   ต่อคน
2. **เมนู "บันทึกรถเข้า-ออก"/"เช็คลิสต์เวรยาม" ขึ้นให้ทุกคนกด แม้ไม่มีสิทธิ์** — เดิมเช็คแค่สวิตช์
   เปิด/ปิดเมนูอย่างเดียว ไม่เช็ค role เลย แก้แล้ว: เพิ่มสิทธิ์ `guard_duty_access` ให้มอบเป็นรายคนได้
   (ปกติเห็นแค่ยาม+แอดมิน)
3. **ปิดประกาศแล้วลบไม่ได้ ปุ่มหายไปเลย** — เดิม "ลบ" แค่ตั้ง active=false แต่ปุ่มลบโชว์เฉพาะตอน
   active=true เท่านั้น พอปิดแล้วเลยลบไม่ได้อีกตลอดไป แก้แล้ว: แยกเป็น "ปิดตอนนี้" (ยังเก็บประวัติ)
   กับ "ลบถาวร" (ลบจริง) คนละปุ่ม กดได้เสมอ
4. **สวิตช์เปิด/ปิดเมนูในหน้าตั้งค่า หน้าตาเพี้ยน** — ป้องกัน CSS ปุ่มเบราว์เซอร์เริ่มต้นแทรกแซง
5. **กดพิมพ์ไฟล์แนบ ต้อง Ctrl+P เอง** — แก้ให้กดปุ่ม "พิมพ์" แล้วเปิดหน้าต่างพิมพ์ทันที ไม่ต้องเดา
6. **แนบ PDF/รูปไม่ได้เลย (แย่ลงจากรอบก่อนที่แนบได้แค่ 1 ไฟล์)** — สร้างคอมโพเนนต์แนบไฟล์กลาง
   (`AttachmentPicker`) ใช้มาตรฐานเดียวกันทั้งแอป (สร้างเอกสาร/แนบเพิ่มทีหลัง/รายงานผลงาน) แนบได้
   หลายไฟล์พร้อมกัน ลบไฟล์ที่เลือกผิดออกได้ก่อนส่ง และถ้าไฟล์ไหนอัพโหลดพลาดจะรายงานชื่อไฟล์ที่พลาด
   ชัดเจน ไม่ใช่ดูเหมือน "พังหมด"
7. **รูปภาพ/ไฟล์แนบตอนดูตัวอย่าง ใหญ่เกินจอ ต้องเลื่อนดู** — จำกัดขนาดตามความสูงหน้าจอ (viewport)
   ทั้งในหน้าเอกสารและหน้าดูรูปรถเข้า-ออก ปรับตามขนาดเครื่องที่ใช้อัตโนมัติ
8. **สร้างเวรให้ครูแล้ว แต่ครูล็อกอินเข้ามาไม่เห็นเมนูเวรเลย** — ตรวจสอบแล้วพบว่าสวิตช์ "เวรครู" ในหน้า
   ตั้งค่าเมนูถูกปิดอยู่ (แยกจากหน้า "จัดการเวรครู" ของแอดมินซึ่งไม่ได้ผูกกับสวิตช์นี้) เปิดสวิตช์นี้แล้ว
   เมนูจะกลับมาให้ทุกคนเห็น

## ฟีเจอร์ใหม่ตามที่ขอ

- **ดูรูปรถเข้า-ออกตามวันที่** — มีปุ่ม "วันนี้" + เลือกวันที่จากปฏิทินได้ ไม่ต้องไล่ดูรูปเป็นร้อยรูปรวมกัน
- **ค่าเริ่มต้นเวลาเวร 07:00 / 17:00** (จากเดิม 07:30)
- **Flow เวรครูครบวงจรตามที่อธิบายมา**: ครูเวรถ่ายภาพได้หลายรูป (ไม่บังคับ) + ลงลายมือชื่อได้
  (รองรับกรณีมีครูเวรร่วม 2 คนเซ็นพร้อมกัน) → หัวหน้าเวรประจำวันเข้ามาตรวจ เขียนความเห็น ลงชื่อกำกับ
  → ส่งต่อให้ ผอ. ลงนามรับทราบขั้นสุดท้ายได้ (ทดสอบ flow เต็มรูปแบบแล้วว่าทำงานถูกต้องจริง)

## ยังไม่ได้ทำในรอบนี้ (คิวถัดไป)

- ไอคอน PWA + ตั้งชื่อแอปเป็น "SNKDocSystem" สำหรับหน้าจอ Home/ทางลัด
- คำแนะนำตั้งค่า Push Notification ผ่าน Caddy + DuckDNS (VAPID keys ใน .env)
- ระบบแปลภาษาให้ครบทุกจุด (ตอนนี้สลับภาษาเปลี่ยนแค่ navbar บางส่วน เนื้อหาในหน้ายังไม่ครบ) +
  หน้า admin แก้คำแปลเองได้ทุกจุดแบบดิกชันนารี
- ปรับฟอร์มอีเมลรายงานผลสำรองข้อมูลประจำคืนให้ดูดีขึ้น
- ปรับ log สำรองข้อมูลให้อ่านง่ายขึ้น + หน้า log ภาพรวมระบบแบบเรียลไทม์ + สถานะ server
- Popup/modal เลือกบุคคลแบบค้นหาได้ (แทน dropdown ธรรมดา) สำหรับหน้าที่ต้องเลือกจากคนหลักร้อยคน
  (เช่น มอบหมายงาน, ตั้งหัวหน้าเวร) — ตอนนี้บางจุดใช้ช่องค้นหาแล้ว (หน้าผู้ใช้งาน, ส่งต่องาน) แต่ยังไม่
  ครบทุกจุด

## รอบเพิ่มเติม (Push Notification + Dropdown + PWA Icon + คำแปล)

### บั๊กที่แก้ไข

1. **Push Notification ใช้งานไม่ได้เลย** — สาเหตุ: `.env` ไม่ได้ตั้งค่า VAPID keys (ยังว่างอยู่) สร้างคีย์
   จริงให้แล้วและใส่ไว้ใน `.env` ทำงานได้ทันที
2. **พบบั๊กร้ายแรงระหว่างตรวจสอบ**: ตัวอย่างในไฟล์ `.env.example` เดิมเขียน `VAPID_SUBJECT` เป็นอีเมลเปล่าๆ
   (ไม่มี `mailto:` นำหน้า) ซึ่งไลบรารี web-push จะโยน error ทันทีถ้าใส่แบบนี้จริง และโค้ดเดิมไม่ได้ครอบ
   try/catch ไว้ — ถ้าใครกรอกตามตัวอย่างเป๊ะๆ **เซิร์ฟเวอร์ทั้งระบบจะล่มตั้งแต่เริ่มต้น** แก้แล้วทั้งสองจุด
   (แก้ placeholder + เติม mailto: ให้อัตโนมัติถ้าลืมใส่ + ครอบ try/catch กันพังทั้งระบบ)
3. **Dropdown เลือกคน (300 กว่าคน) ยังเป็นแบบเก่าอยู่หลายจุด** — โดยเฉพาะหน้าจัดการเวรครู (ผู้รับผิดชอบหลัก,
   หัวหน้าเวรประจำวัน) และหน้าเวร (เลือกครูเวรร่วม) แก้ให้ใช้ตัวเลือกบุคคลแบบพิมพ์ค้นหาได้ตัวเดียวกันทั้งแอป
   แล้ว (คอมโพเนนต์ `PersonSelect`/`PersonMultiSelect`) ครอบคลุมทุกจุดที่เคยเป็น dropdown ธรรมดา:
   จัดการเวรครู, เวรวันนี้ (ครูเวรร่วม), หัวหน้าแผนก, มอบหมายงานต่อ, ส่งต่องานเอกสาร

### ฟีเจอร์ใหม่ตามที่ขอ

- **ไอคอน PWA มืออาชีพ + ชื่อแอป "SNKDocSystem"** — ออกแบบไอคอนใหม่ (โทนน้ำเงินของระบบ + ตราติ๊กถูก)
  สร้างไฟล์ครบตามที่ iOS/Android/เดสก์ท็อปต้องการ (`apple-touch-icon.png` สำคัญมากสำหรับ iOS
  "เพิ่มไปยังหน้าจอโฮม" เพราะ iOS ไม่อ่านไอคอนจาก manifest.webmanifest) อัปเดตชื่อทั้งใน manifest,
  หน้าล็อกอิน, แถบข้าง, และข้อความแจ้งเตือน
  - หมายเหตุเรื่อง iOS: "เพิ่มไปยังหน้าจอโฮม" กับ "ติดตั้งแอป" (Android) เป็นพฤติกรรมที่ต่างกันโดยธรรมชาติ
    ของแพลตฟอร์ม — iOS Safari ไม่รองรับหน้าต่างติดตั้งแบบ Android เลย (ข้อจำกัดของ Apple ไม่ใช่บั๊กของเรา)
    แต่หลังเพิ่มไปหน้าจอโฮมแล้ว จะเปิดเป็นแอปเต็มจอเหมือนแอปจริงได้ปกติ
- **ปุ่มทดสอบการแจ้งเตือน** ในหน้า "การแจ้งเตือน" — กดแล้วส่ง push ทดสอบหาตัวเองทันที ไม่ต้องรอเหตุการณ์จริง
  พร้อมคำอธิบายข้อจำกัดของ iOS (ต้องเพิ่มหน้าจอโฮมก่อน + iOS 16.4 ขึ้นไป)
- **หน้า "จัดการคำแปล"** (แอดมินเท่านั้น) — แก้คำแปลของทุกเมนู/ปุ่ม/สถานะหลักได้เองทั้ง 3 ภาษา (ไทย/อังกฤษ/จีน)
  โดยไม่ต้องแก้โค้ด บันทึกแล้วมีผลกับทุกคนทันที ค้นหาคำได้ มีปุ่มรีเซ็ตกลับค่าเริ่มต้นรายคำ
  (ทดสอบแล้วว่าบันทึก/โหลดค่าที่แก้ไขทำงานถูกต้อง)
- **เพิ่มคำแปลให้หน้าเวรครู** ครบทุกข้อความหลัก (เดิมเป็นตัวอย่างที่ยกมาว่าแปลไม่ครบ) เป็นตัวอย่างการใช้งาน
  ระบบคำแปลใหม่ — หน้าอื่นๆ ยังไม่ได้ทำครบ 100% แต่โครงสร้าง/วิธีทำเหมือนกันหมด ทำต่อได้เรื่อยๆ ทีละหน้า

### ยังไม่ได้ทำ

- แปลข้อความให้ครบทุกหน้า 100% (ทำโครงสร้าง + หน้าเวรครูเป็นตัวอย่างแล้ว ที่เหลือเป็นงานทำซ้ำทีละหน้า)
- ฟอร์มอีเมลรายงานผลสำรองข้อมูลให้ดูดีขึ้น
- Log สำรองข้อมูลให้อ่านง่ายขึ้น + หน้า log ภาพรวมระบบเรียลไทม์ + สถานะ server

## รอบเพิ่มเติม (Push Notification บน iOS + หัวกระดาษกำหนดเอง)

### บั๊กที่แก้ไข

1. **Push Notification ยังไม่ทำงานบน iPad แม้เพิ่มไปหน้าจอโฮมถูกต้องแล้ว** — เจอสาเหตุจริง: โค้ดเดิมไป
   ดึงข้อมูล public key จากเซิร์ฟเวอร์ก่อน แล้วค่อยขอ permission ทีหลัง ซึ่ง iOS Safari เข้มงวดมากเรื่อง
   "user activation" — พอมีการติดต่อเซิร์ฟเวอร์คั่นกลางระหว่างกดปุ่มกับขอ permission มันจะถือว่าไม่ใช่การ
   กดปุ่มโดยตรงแล้วปฏิเสธเงียบๆ โดยไม่มี popup ขึ้นมาเลยด้วยซ้ำ (อธิบายได้ว่าทำไมแอปไม่เคยโผล่ใน
   ตั้งค่า iOS > การแจ้งเตือน) แก้โดยสลับให้ขอ permission เป็นคำสั่งแรกสุดทันทีที่กดปุ่ม ไม่มีการติดต่อ
   เซิร์ฟเวอร์คั่นกลางอีกต่อไป — **ถ้าเคยลองกดปุ่มนี้มาก่อนอัปเดตนี้ แนะนำให้ลบไอคอนออกจากหน้าจอโฮมแล้ว
   เพิ่มใหม่อีกครั้งก่อนลองใหม่** (มีข้อความเตือนนี้โชว์ในแอปให้แล้วด้วย)

### ฟีเจอร์ใหม่ตามที่ขอ

- **หัวกระดาษเอกสารกำหนดเองได้** — เดิม fix คำว่า "บันทึกข้อความ" ตายตัวทุกประเภทเอกสาร ตอนนี้แก้ไขได้ที่
  หน้า "ประเภทเอกสาร" ต่อประเภทเลย (เช่น "หนังสือเข้า", "คำสั่งโรงเรียน", "ประกาศ" ฯลฯ) ประเภทเอกสารเดิม
  ที่มีอยู่แล้วในระบบจะใช้คำว่า "บันทึกข้อความ" เป็นค่าเริ่มต้นไปก่อน (ต้องเข้าไปแก้เองทีละประเภทถ้าต้องการ
  เปลี่ยน เพราะระบบเดิมไม่มีข้อมูลนี้อยู่ก่อน) ส่วนข้อมูลตัวอย่าง (data_seed) ที่ใช้ตอนติดตั้งใหม่ ตั้งค่า
  หัวกระดาษที่เหมาะสมให้แต่ละประเภทไว้แล้ว

## รอบเพิ่มเติม (บั๊กแจ้งเตือนไม่เกี่ยวข้อง + งานค้างทั้งหมด 5 ข้อ)

### บั๊กที่แก้ไข (สำคัญ)

1. **แจ้งเตือนเอกสารที่ไม่เกี่ยวข้อง + กดแล้วค้างหน้าโหลดตลอดกาล** — เจอ 2 บั๊กจริง:
   - หน้ารายละเอียดเอกสาร ถ้าโหลดข้อมูลไม่สำเร็จ (เช่น 403 ไม่มีสิทธิ์ดู) จะค้างที่ "กำลังโหลด..." ตลอดไป
     ไม่มีทางรู้ว่าเกิดอะไรขึ้น แก้แล้วให้ขึ้นข้อความอธิบายชัดเจนพร้อมปุ่มย้อนกลับ
   - **บั๊กที่ใหญ่กว่า**: ตอนส่งต่องานให้ "แผนก" (เช่น หัวหน้าสำนักงานส่งให้แผนกการเงิน) เดิมระบบจะให้
     สิทธิ์ดู + แจ้งเตือน **สมาชิกทุกคนในแผนกนั้น** ไม่ใช่แค่หัวหน้าแผนกอย่างที่ควรเป็น ทำให้พนักงานทั่วไป
     ในแผนกได้รับแจ้งเตือนเอกสารที่ไม่เกี่ยวกับตัวเองโดยตรง แก้ไขให้เห็น/แจ้งเตือนเฉพาะหัวหน้าแผนกเท่านั้น
     ตรงตามที่ตกลงกันไว้ (หัวหน้าแผนกเป็นผู้ตัดสินใจส่งต่อให้ลูกน้องคนไหนอีกที) — เพิ่มการตรวจสอบด้วยว่า
     ถ้าแผนกไหนยังไม่ได้ตั้งหัวหน้าไว้ จะส่งต่อไปไม่ได้เลย (กันเอกสารหายไม่มีใครเห็น) ทดสอบยิง API ยืนยัน
     ครบทั้ง 2 เคสแล้ว (ส่งไปแผนกไม่มีหัวหน้า → error ชัดเจน / ตั้งหัวหน้าแล้วส่งได้ปกติ)

### งานที่เคยค้างไว้ ทำครบทั้ง 5 ข้อแล้ว

2. **แปลภาษาเพิ่มเติม** — ทำเพิ่มครบทุกหน้าหลักที่ใช้งานบ่อย: หน้าเอกสาร (สร้าง/รายละเอียด/รายการ),
   ผู้ใช้งาน, สำรองข้อมูล, ยาม, รถเข้า-ออก, การแจ้งเตือน, ประกาศระบบ, แดชบอร์ด, ล็อกอิน, เวรครู
   (หมายเหตุ: ข้อความในแบบฟอร์มเอกสารที่พิมพ์จริง เช่น "บันทึกข้อความ/เรียน/เรื่อง" ตั้งใจไม่แปล เพราะเป็น
   รูปแบบเอกสารราชการไทยมาตรฐาน ถ้าแปลจะทำให้เอกสารที่พิมพ์จริงดูแปลก ส่วนหน้าตั้งค่าเชิงลึกของแอดมิน
   บางหน้า (จัดการเวรครู, ตั้งค่าระบบ, ประเภทเอกสาร) ยังไม่ได้ทำครบ 100% แต่โครงสร้างเหมือนกันหมด)
3. **ฟอร์มอีเมลรายงานผลสำรองข้อมูล** — ออกแบบใหม่ทั้งหมด มีหัวข้อ สีสถานะ (เขียว/แดง) ตารางข้อมูลอ่านง่าย
   แทนข้อความยาวเป็นพรืดแบบเดิม
4. **Log สำรองข้อมูลอ่านยาก** — เปลี่ยนจากข้อความดิบเป็นการ์ดแยกรายการ มีไอคอนสถานะ วันที่ ผลลัพธ์ชัดเจน
5. **หน้าสถานะระบบ/Log ภาพรวมเรียลไทม์** — สร้างใหม่ (เมนู "สถานะระบบ/Log") แสดง uptime, การใช้
   หน่วยความจำ, จำนวนชุดสำรองข้อมูล, จำนวนเอกสาร/ผู้ใช้งาน และกิจกรรมล่าสุดในระบบ รีเฟรชอัตโนมัติทุก
   5 วินาที (กดหยุด/เริ่มรีเฟรชเองได้) — มีคำอธิบายในหน้าด้วยว่าเมนูนี้ใช้ไม่ได้ถ้า server ล่มจนเปิดเว็บไม่ได้เลย
6. **เห็น log ทาง cmd/console เผื่อ server ล่ม** — อธิบายไว้ใน README ชัดเจนว่าทำไมเด้งหน้าต่างอัตโนมัติแบบ
   สมัยก่อนทำไม่ได้ (ข้อจำกัดของ Windows Service ไม่มีหน้าต่างเลยตั้งแต่ต้น) พร้อมวิธีตั้งค่า NSSM ให้เขียน
   log เก็บไว้เป็นไฟล์ (`AppStdout`/`AppStderr`) และคำสั่งดู log สดๆ แบบ tail ผ่าน PowerShell

## รอบเพิ่มเติม (บั๊กยิบย่อย + i18n เพิ่ม + backup preview)
### บั๊กที่แก้ไข

1. **Date picker ต้องกดที่ icon เท่านั้น** — เพิ่ม `showPicker()` ให้ทุกช่องวันที่ในระบบ กดตรงไหนของ
   ช่องก็เปิดปฏิทินได้เลย ไม่ต้องเล็งกดที่ไอคอนเล็กๆ
2. **Dropdown เลือกคนในหน้า "จัดการเวรครู" โผล่แค่นิดเดียว** — เจอสาเหตุจริง: กล่องรายชื่อที่เป็น
   position:absolute ถูกกล่อง overflow-hidden ของการ์ดที่ครอบอยู่ตัดขาด (การ์ดใช้ overflow-hidden เพื่อปัด
   มุมตารางให้สวย) แก้โดยเปลี่ยนให้กล่องรายชื่อ render ผ่าน portal ไปที่ body โดยตรง ไม่มีทางโดน parent
   ตัดอีกเลยไม่ว่าจะเอาไปวางตรงไหนในแอป (แก้ทั้ง PersonSelect และ PersonMultiSelect ที่ใช้ทั่วทั้งระบบ)
3. **ฟอร์มเอกสารเสี่ยงตัวอักษรทะลุ** — ตรวจสอบแล้วพบว่า `.a4-row`/`.a4-title` (ใช้กับหัวกระดาษที่กำหนดเอง
   และป้ายชื่อฟิลด์) ไม่มีการป้องกันคำยาวทะลุกรอบเหมือนช่องเนื้อหาหลัก แก้ให้ครอบคลุมทั้งหน้าแล้ว
   พร้อมจำกัดความยาวชื่อประเภทเอกสาร (100 ตัวอักษร) และหัวกระดาษ (40 ตัวอักษร) กันไว้อีกชั้น

### ฟีเจอร์ใหม่/ปรับปรุง

- **แปลภาษาเพิ่ม**: หน้า "จัดการเวรครู" และ "จัดการคำแปล" ครบทั้ง 3 ภาษาแล้ว (ก่อนหน้านี้เป็นไทยล้วน)
- **ดูรายละเอียดในชุดสำรองข้อมูลได้** — กดไอคอนรูปตาข้างชื่อชุดสำรอง (ทั้งฝั่งเครื่องนี้และ NAS) เห็นจำนวน
  เอกสาร/ผู้ใช้งาน ณ ตอนนั้น + รายชื่อไฟล์แนบทั้งหมด เปิดดูรูป/PDF ได้เลยถ้าเป็นชุดที่อยู่บนเครื่องนี้
- **อธิบายกลไกสำรองข้อมูลชัดเจนใน README** — ลบจากเครื่อง server ไม่กระทบ NAS เลย, หน้าเว็บยังเห็น/กู้คืน
  ชุดที่ลบจากเครื่องแล้วได้ตราบใดที่ยังอยู่บน NAS, เพิ่มค่าเริ่มต้นการเก็บจาก 30 เป็น 60 ชุด
- **เพิ่มคำอธิบาย role vs แผนก** ในหน้าผู้ใช้งาน ให้เข้าใจง่ายขึ้นว่าทำไมต้องมีทั้ง 2 อย่าง (ไม่ได้รื้อโครงสร้าง
  เพราะเสี่ยงกระทบระบบสิทธิ์/นำเข้าผู้ใช้หลายจุดตามที่ตกลงกันไว้)

### ยังไม่ได้ทำ (คิวถัดไป)

- หน้าแก้ไขเอกสารแบบเต็มตอนถูกตีกลับ (ใช้ฟอร์มเดียวกับหน้าสร้าง ดึงข้อมูลเดิมมาให้แก้/ลบ-เปลี่ยนไฟล์ PDF ได้
  พร้อมลง log ว่าใครแก้แต่ไม่ลงรายละเอียด) — ยังเป็นแค่ "ส่งใหม่" แบบเดิม
- กู้คืนเอกสารจากถังขยะได้ภายใน 1 เดือนโดยผู้ลบ
- แปลภาษาหน้าอื่นที่เหลือให้ครบ 100% (ทำไปมากแล้วในหลายรอบ แต่ยังไม่ครบทุกหน้า)


## รอบเพิ่มเติม (ฟอร์ม PDF 12 แบบ + พรีวิวเรียลไทม์ + แก้ไขเอกสารตีกลับ + กู้คืนถังขยะ + บั๊ก mobile notification)

### บั๊กที่แก้ไข

1. **เมนู 3 รายการค้างภาษาเดียว ไม่ว่าจะสลับภาษาไหน** — คีย์คำแปล `navManageDuty`, `navTranslations`,
   `navSystemLog`, `navOpsGroupTitle` ไม่เคยถูกใส่ไว้ในพจนานุกรมเลยตั้งแต่แรก (ไม่ใช่บั๊กเฉพาะภาษา — ทุกภาษา
   โดนหมด) แก้แล้วครบทั้ง 3 ภาษา + ตรวจสอบทั้งระบบว่าคีย์อื่นครบ 100% แล้ว (373 คีย์ตรงกันทุกภาษา)
2. **มือถือกดเข้าแอพผ่าน push notification แล้วโหลดไม่ขึ้น/ค้าง** — เจอ 3 สาเหตุร่วมกัน: (1) service worker
   เปิดหน้าต่างใหม่ทุกครั้งแทนที่จะโฟกัสแท็บเดิมที่เปิดอยู่แล้ว ทำให้บูตแอพซ้ำสอง (2) เรียก API ไม่มี timeout
   เลยสักที่ — ถ้ามือถือเพิ่งตื่นจาก background ตอนเน็ตยังไม่พร้อม คำขอค้างเงียบๆ ได้นานมาก (3) ไม่มีการเช็ค
   สถานะล็อกอินใหม่ตอนแอพกลับมาอยู่หน้าจอ แก้ครบทั้ง 3 จุด + เพิ่มหน้าจอโหลด (splash) กันจอขาวเปล่าตอนบูต

### งานที่เคยค้างไว้ ทำครบทั้ง 3 ข้อแล้ว

3. **ฟอร์ม PDF ทั้งหมดในโรงเรียน (12 แบบ)** — ตรวจไฟล์ที่แนบมาแล้วทำเป็นประเภทเอกสารดิจิทัลครบทั้ง 12 แบบ
   (แบบขอใช้รถยนต์, ใบตรวจรับวัสดุ, ใบขอจัดซื้อ/จัดจ้าง, แบบฟอร์มบันทึกการเปลี่ยนเวร, ใบเบิกเงินสวัสดิการ
   การศึกษาบุตร กท.16 ช., ใบเบิกเงินสวัสดิการค่ารักษาพยาบาล กท.4 ช., แบบขอหนังสือรับรอง, ใบจัดสอนแทนแนบท้าย
   ใบลา, ใบขอเบิกวัสดุ, แบบขออนุญาตดำเนินการโครงการ/งาน — อีก 2 แบบคือ "บันทึกข้อความ"/"ใบลา" มีอยู่แล้วใน
   ระบบ เลยเสริมของเดิมแทนสร้างซ้ำ) เพิ่มชนิดฟิลด์ใหม่ "ตารางรายการ" รองรับฟอร์มที่มีตารางกรอกได้หลายแถว
   (เพิ่ม/ลบแถวเองได้ ไม่จำกัดจำนวนไว้ล่วงหน้า) พร้อม**พรีวิวเอกสารแบบเรียลไทม์**ข้างฟอร์มตอนกรอก (อัปเดตทันที
   ตามที่พิมพ์ ใช้ตัวเรนเดอร์เดียวกับตอนพิมพ์จริง รับประกันว่าตรงกันเป๊ะ)
4. **หน้าแก้ไขเอกสารแบบเต็มตอนถูกตีกลับ** — สร้างหน้าใหม่ `/documents/:id/edit` ใช้ชุดฟิลด์เดียวกับหน้า
   สร้างเอกสารทุกอย่าง (รวมพรีวิวเรียลไทม์) ดึงข้อมูลเดิมมาเติมให้ครบ แก้ไขได้จริงทุกช่อง ลบไฟล์แนบเดิม/แนบไฟล์
   ใหม่ได้ ลง log ว่าใครแก้แต่ไม่ลงรายละเอียดตามที่ขอ (ไม่ใช่แค่ "ส่งใหม่" แบบเดิมอีกต่อไป)
5. **กู้คืนเอกสารจากถังขยะได้ภายใน 30 วันโดยผู้ที่ตีกลับเอง** — ตอนตีกลับ/ไม่อนุมัติ ระบบบันทึกว่าใครเป็นคน
   ตีกลับและเมื่อไหร่ กู้คืนได้เฉพาะคนนั้น (หรือแอดมิน) ภายใน 30 วัน พ้นกำหนดกู้คืนไม่ได้อีก กู้คืนแล้วกลับไป
   อยู่ขั้นตอนตรวจสอบเดิมที่ค้างอยู่ ไม่ต้องเริ่มใหม่ทั้งหมด

### ยังไม่ได้ทำ (คิวถัดไป)

- ฟอร์มราชการที่ซับซ้อนมาก 2 แบบ (กท.16 ช., กท.4 ช.) ทำเป็นดิจิทัลแบบครอบคลุมประเด็นหลักแล้ว แต่ยังไม่ตรงกับ
  ต้นฉบับราชการทุกช่องย่อย 100% — ถ้าต้องยื่นหน่วยงานภายนอกโดยตรง แนะนำตรวจทานช่องให้ครบก่อนใช้งานจริง
- Full production build (`npm run build`) รันไม่ได้ในสภาพแวดล้อมที่ใช้พัฒนารอบนี้ (ขาด native binding ของ
  `rolldown`/`oxlint` สำหรับ Linux และไม่มีเน็ตให้ดึงมาลงเพิ่ม) — ตรวจสอบโค้ดด้วย `tsc -b --force` (TypeScript
  strict) ผ่านสะอาด 100% แทน แนะนำรัน `npm run build` อีกครั้งที่เครื่องพัฒนาจริงก่อน deploy เพื่อความชัวร์

## รอบเพิ่มเติม (บั๊กสิทธิ์หัวหน้าแผนก + เส้นทางอนุมัติกำหนดเอง + แดชบอร์ด ผอ. + แก้ไขระหว่างรอตรวจกรอง + ฟอร์ม PDF คงที่ + กู้คืนสำรองข้อมูลไม่ล่ม)

### บั๊กสำคัญที่สุดในรอบนี้

1. **หัวหน้าแผนก (เช่นหัวหน้าการเงิน) เปิดดูเอกสารที่ส่งมาแล้วไม่ขึ้น** — เจอความไม่สอดคล้องกันจริงในโค้ด:
   จุดแสดงรายการ/นับแจ้งเตือนอนุญาต "สมาชิกทุกคนในแผนก" แต่จุดเปิดดูเอกสารจริง (`canView`) อนุญาตเฉพาะ
   "หัวหน้าแผนกเท่านั้น" ถ้าข้อมูลสองจุด (สมาชิกแผนก vs หัวหน้าแผนก) ไม่ตรงกันเป๊ะจะเจอปัญหาทันที — แก้ให้
   ทุกจุดที่เกี่ยวข้อง (ดู/รับทราบ/ปิดงาน/นับแจ้งเตือน) ยึดกติกาเดียวกัน พร้อมเช็คสำรองสองชั้นกันข้อมูลไม่ตรงกัน
2. **กู้คืนข้อมูลสำรองแล้วเว็บล่มนาน** — สาเหตุจริง 2 จุด: (1) โค้ดเดิมเขียน log ไปที่ DB ที่เพิ่งปิด+สลับไฟล์
   ไปแล้ว ทำให้ error ทุกครั้งทั้งที่ไฟล์กู้คืนสำเร็จจริง (2) รีสตาร์ทพึ่งพา NSSM ซึ่งไม่มีบนเครื่อง dev เลย
   ทำให้เซิร์ฟเวอร์ตายค้าง — แก้ทั้งคู่: log แยกไฟล์ text ต่างหาก + เปลี่ยนมาเปิด-ปิด DB connection สดใน
   โปรเซสเดียวกันแทนการ restart ทั้งแอป

### ฟีเจอร์ใหม่ตามที่ขอ

3. **เส้นทางอนุมัติแบบกำหนดเอง** — ตั้งค่าที่ประเภทเอกสารได้ว่าจะ "ปกติ" (ผ่านหัวหน้าสำนักงาน→ผอ.) หรือ
   "ส่งตรงถึงหัวหน้าแผนก" (ข้ามทั้งคู่) ล็อกแผนกตายตัวหรือให้ผู้จัดทำเลือกเองก็ได้ มีระบบเตือนถ้าแผนกปลายทาง
   ยังไม่มีหัวหน้าแผนก
4. **แดชบอร์ด ผอ.** — ตัดสถานะ "รอตรวจกรอง" (ยังไม่ถึงคิว ผอ.) ออก เหลือเฉพาะที่เกี่ยวกับท่าน + เพิ่มการ์ด
   "เอกสารค้างระหว่างดำเนินการนานที่สุด" บอกว่ารอใคร/แผนกไหน ค้างกี่วัน กดเข้าอ่าน log ต่อได้เลย
5. **แก้ไขเอกสารได้ตอนสถานะ "รอตรวจกรอง"** — ไม่ต้องรอถูกตีกลับก่อนแล้วค่อยแก้ไขได้อีกต่อไป (แก้ไม่เปลี่ยน
   สถานะ/ไม่เข้าคิวใหม่) พอเริ่มพิจารณาแล้วถึงจะแก้ไม่ได้จนกว่าจะถูกตีกลับ
6. **ระบบฟอร์ม PDF คงที่** — โฟลเดอร์ `server/static-forms/` (มี README วิธีตั้งชื่อไฟล์) สำหรับฟอร์มที่ยังไม่
   รองรับกรอกดิจิทัล (เช่น กท.16 ช./กท.4 ช. ที่ซับซ้อนมากและส่งออกหน่วยงานภายนอก) ตั้งค่าที่ประเภทเอกสารแล้ว
   ผู้ใช้งานจะเห็นพรีวิว PDF + ปุ่มดาวน์โหลด/สั่งพิมพ์แทนฟอร์มดิจิทัล

### บั๊กยิบย่อยที่แก้เพิ่ม

7. ลบประเภทเอกสารไม่ได้ (soft-delete ทำงานจริงแต่ UI ไม่กรองออก) — แก้แล้ว
8. พิมพ์เอกสารมี 2 หน้า หน้า 2 ว่างเปล่ามีลายเซ็นลอย (บั๊ก CSS position:absolute + min-height คลาสสิก) —
   แก้แล้ว พร้อมเพิ่มการเตือนก่อนพิมพ์ถ้าเนื้อหายาวเกิน 1 หน้าจริง
9. พรีวิวเอกสารมุมโค้งมนไม่เท่ากัน (scale ตายตัวไม่พอดีจอ) — แก้เป็นคำนวณ scale อัตโนมัติ ช่วยเรื่องมือถือ
   ในหน้านี้ไปด้วย
10. สถานะดิสก์ไม่ขึ้นบนเครื่อง server (Windows) — เพิ่ม fallback ด้วย `wmic`

### ยังไม่ได้ทำ (คิวถัดไป)

- Responsive มือถือกว้างๆ ทั้งแอป — แก้เฉพาะจุดที่เจอสาเหตุชัดในรอบนี้ (พรีวิวเอกสาร) ส่วนหน้าอื่นยังไม่ได้
  ตรวจทุกหน้าเพราะไม่มีอุปกรณ์มือถือจริงทดสอบ ถ้าเจอหน้าไหนมีปัญหาแจ้งชื่อหน้าเพิ่มได้
- ฟอร์มราชการ 2 แบบ (กท.16ช./กท.4ช.) ยังเป็นฟอร์มดิจิทัลแบบครอบคลุมประเด็นหลัก ไม่ตรงต้นฉบับ 100% ทุกช่อง —
  ถ้าต้องยื่นหน่วยงานภายนอกตรงๆ แนะนำใช้ระบบฟอร์ม PDF คงที่ใหม่ (ข้อ 6) แทน
- Full production build (`npm run build`) ยังรันไม่ได้ในสภาพแวดล้อมพัฒนา (ขาด native binding, ไม่มีเน็ต) —
  ตรวจสอบด้วย `tsc -b --force` (TypeScript strict) ผ่านสะอาด 100% แทน แนะนำรัน `npm run build` อีกครั้งที่
  เครื่องพัฒนาจริงก่อน deploy

---

## เนื้อหาที่รวมมาจาก CHANGES_FROM_REVIEW.md

# สรุปการแก้ไขจากรีวิวโค้ด (school-docsystem-vite)

ไฟล์นี้สรุปทุกจุดที่แก้ไขแล้ว และจุดที่ยังต้องทำต่อ เทียบกับที่คุณแจ้งมา

## แก้ไขแล้ว

### 1. โหมดสี (มืด/สว่าง) แสดงผิดตามที่ตั้งไว้
**ไฟล์:** `web/src/index.css`
**สาเหตุจริง:** โปรเจกต์ใช้ Tailwind CSS v4 ซึ่งเปลี่ยนพฤติกรรมของ `dark:` ให้อิงตาม
การตั้งค่าโหมดมืดของ "เบราว์เซอร์/OS ผู้ใช้" โดยอัตโนมัติ แทนที่จะอิงตาม class `.dark`
บน `<html>` เหมือน v3 — ทำให้ `ThemeContext` ที่สลับ class `.dark` ไม่มีผลกับ
`dark:text-...`, `dark:bg-...` เลย สีเลยดูขัดกันไปมาตามเครื่องแต่ละคน
**วิธีแก้:** เพิ่ม `@custom-variant dark (&:where(.dark, .dark *));` ต่อจาก
`@import "tailwindcss";` บรรทัดเดียว ทำให้ `dark:` กลับมาอิงตาม class ที่แอปสลับให้จริง
ทั้งหมด — นี่คือจุดเดียวที่แก้ปัญหาตัวอักษรอ่านไม่ออกและปัญหาธีมกำหนดเองไม่ค่อยมีผล
ได้เกือบทั้งหมด

### 2. ข้อความทะลุกรอบ/ทะลุขอบกระดาษ
**ไฟล์:** `web/src/pages/documents/DocumentDetail.tsx`
- ช่องแสดงค่าฟิลด์ในหน้ารายละเอียดเอกสาร: เพิ่ม `break-words` (เดิมมีแค่
  `whitespace-pre-wrap` ซึ่งไม่ตัดคำที่ไม่มีช่องว่างเลย)
- ผังหน้ากระดาษแบบลากวาง (`PrintLayoutView`): เดิมใช้ `whitespace-nowrap` ไม่มี
  การจำกัดความกว้าง ข้อความยาวจึงทะลุขอบกระดาษได้เสมอ ไม่ว่าจะวางตำแหน่งไหน ตอนนี้
  คำนวณ `max-width` จากระยะถึงขอบกระดาษที่ใกล้ที่สุด และให้ตัดคำได้

### 3. บัญชียามเห็นเมนูทั้งหมด (ควรเห็นแค่ 2 เมนู)
**ไฟล์:** `web/src/components/Layout.tsx`
เดิมไม่มีการเช็ค role ยามเลยในเมนู ใช้ `show: true` แบบไม่มีเงื่อนไขในหลายเมนูหลัก
ตอนนี้ถ้า role เป็น `guard` จะเห็นเฉพาะ "เช็คลิสต์เวรยาม" และ "บันทึกรถเข้า-ออก"
เหมือนระบบเดิม ไม่เห็นเมนูแดชบอร์ด/เอกสาร/แอดมินใดๆ เลย

### 4. ตัวเปิดปิด "เช็คลิสต์เวรยาม" ซ้ำกันอยู่ 2 ที่ (คนละระบบ ไม่เชื่อมกัน)
**ไฟล์:** `web/src/pages/admin/AdminSettings.tsx`
หน้า "ตั้งค่าระบบ" มีอีกตัวแปรเก่า `settings.guardChecklistEnabled` ที่ backend
ไม่ได้ใช้บังคับสิทธิ์จริงแล้ว (ของจริงคือ `featureToggles.guardChecklist` ในหน้า
"เปิด/ปิดเมนูฟังก์ชัน") จึงลบตัวซ้ำที่ไม่มีผลออกจากหน้าตั้งค่าระบบ เหลือจุดควบคุม
เดียวที่หน้าเปิด/ปิดเมนูฟังก์ชัน

### 5. ระบบ Preview/พิมพ์แบบ A4 หายไป (คืนความสามารถจากของเดิม)
**ไฟล์:** `web/src/pages/documents/DocumentDetail.tsx`, `web/src/index.css`,
`web/index.html`
เดิมกดพิมพ์แล้ว `window.print()` จะพิมพ์ "ทั้งหน้าเว็บ" ตรงๆ ไม่มีฟอร์ม A4 ไม่มี
หน้าต่าง preview เลย ตอนนี้คืนกลับมา:
- เพิ่ม `<div id="printArea">` ถาวรใน `index.html` และ CSS `@media print` แบบเดิม
  ที่ซ่อนทุกอย่างยกเว้น `#printArea` ตอนสั่งพิมพ์
- สร้างคอมโพเนนต์ `ClassicMemoSheet` — ฟอร์มบันทึกข้อความ A4 แบบเดิม (หัวกระดาษ
  ชื่อโรงเรียน / ที่-วันที่ / เรื่อง-เรียน / เนื้อหา / บล็อกลงชื่อผู้จัดทำมุมขวาล่าง
  + บล็อกความเห็นผู้เกษียนมุมซ้ายล่างถ้ามี) ใช้เป็นค่าเริ่มต้นสำหรับทุกประเภทเอกสาร
  ที่ยังไม่ได้ตั้งผังหน้ากระดาษแบบลากวางไว้
- เพิ่มปุ่ม "ดูตัวอย่าง/พิมพ์" เปิดหน้าต่าง preview ขนาด A4 ย่อบนหน้าจอ พร้อมปุ่ม
  สั่งพิมพ์จริงในตัว
- ถ้าประเภทเอกสารมีการตั้งผังหน้ากระดาษแบบลากวางไว้แล้ว (ฟีเจอร์ใหม่) ระบบจะยังใช้
  ผังนั้นแทนได้ตามเดิม ไม่ได้ตัดฟีเจอร์ใหม่ทิ้ง แค่ทำให้ "ฟอร์มคลาสสิก" เป็นค่าเริ่มต้น
  แทนที่จะไม่มีอะไรเลย

## ยังไม่ได้แก้ / ต้องตัดสินใจเพิ่ม

### "วันที่หนังสือต้นทาง" เป็นข้อความธรรมดาไม่ใช่ตัวเลือกวันที่
ไม่ใช่บั๊กโค้ด — ฟิลด์นี้ถูกตั้งเป็นชนิด `text` แทนที่จะเป็น `date` ตอนสร้างประเภท
เอกสาร "หนังสือเข้าจากภายนอก" แก้ได้เองที่ Admin → ประเภทเอกสาร → แก้ไขฟิลด์นั้น
เปลี่ยนชนิดเป็น "วันที่"

### ระบบธีมกำหนดเอง (custom)
หลัง fix ข้อ 1 แล้ว ปุ่มเลือกสีหลัก/สีเน้นจะมีผลจริงกับปุ่มและองค์ประกอบที่อ้างอิง
`--color-primary`/`--color-accent` แต่สีพื้นหลัง/ตัวอักษรทั่วไป (เทาต่างๆ ของ
Tailwind) ยังไม่ถูกดึงไปอยู่ใน custom mode เพราะโค้ดส่วนใหญ่ hardcode เป็น
`slate-*` ไม่ใช่ CSS variable ถ้าต้องการให้ "กำหนดเอง" เปลี่ยนได้ทั้งหน้าจริงๆ
(ไม่ใช่แค่ปุ่ม) ต้องรื้อปรับให้ทุกที่อ้างอิง CSS variable แทน ซึ่งเป็นงานเพิ่มอีกก้อนหนึ่ง
แยกจากรอบนี้

### ไอคอนปุ่มเปิด/ปิดในหน้า "เปิด/ปิดเมนูฟังก์ชัน"
ตรวจสอบโค้ดแล้วเป็นสวิตช์ toggle มาตรฐาน (ทรงเดียวกับที่ใช้ทั่วทั้งแอป) ไม่พบ
โค้ดที่ผิดปกติ — รบกวนส่งภาพหน้าจอที่เห็นว่า "แปลกๆ" มาเพิ่ม เผื่อเป็นปัญหาเฉพาะ
เบราว์เซอร์/ธีมที่ยังไม่ได้ลองซ้ำ

### ยังไม่ได้ build จริงในเครื่องนี้
`npx tsc --noEmit` ผ่านสะอาด (ไม่มี type error) แต่ `vite build` ในแซนด์บ็อกซ์นี้
ติดปัญหา native binding ของ `rolldown` ที่ไม่เกี่ยวกับโค้ดที่แก้ (ปัญหาเครื่อง/แพลตฟอร์ม
ของแซนด์บ็อกซ์เอง) แนะนำให้รัน `npm install` แล้ว `npm run build` ในเครื่องคุณเองอีกที
ก่อนขึ้นโปรดักชัน

---

## เนื้อหาที่รวมมาจาก อ่านก่อน.md (แพตช์แก้บั๊ก 3 เรื่อง)

# แพตช์แก้บั๊ก 3 เรื่อง (6 ส.ค. 2569)

## วิธีติดตั้ง
1. สำรองข้อมูลก่อนเสมอ (โฟลเดอร์ `server/data` และ `server/uploads`) — แพตช์นี้ไม่แตะข้อมูลเดิมเลย มีแต่ไฟล์โค้ด
2. คัดลอกไฟล์ในแพตช์นี้ทับไฟล์เดิมที่ตำแหน่งเดียวกันในระบบจริง:
   - `server/lib/permissions.js`
   - `server/server.js`
   - `web/src/pages/documents/DocumentDetail.tsx`
   - `web/src/i18n/dictionaries.ts`
   - `server/public/*` (หน้าเว็บที่ build แล้ว พร้อมใช้ทันที ไม่ต้อง `npm run build` เอง — แต่ถ้าสะดวก แนะนำให้ลอง `npm run build` ที่เครื่องจริงอีกครั้งก่อนใช้งานจริงตามที่ README เดิมแนะนำไว้)
3. รีสตาร์ท server (`node server.js` หรือผ่าน NSSM/PM2 ตามที่ตั้งไว้)

## 1) หัวหน้าแผนกที่สังกัดหลายแผนก เห็นเอกสารของแผนกอื่นทั้งที่ยังไม่ถูกส่งต่อให้
**สาเหตุจริง:** จุดเช็คสิทธิ์ดู/รับทราบ/ปิดงานของ "แผนก" (`canView`, กล่องข้อความ inbox, ปุ่มรับทราบ, ปุ่มเสร็จสิ้น, แดชบอร์ด)
เช็คว่า user เป็น **สมาชิก** ของแผนกนั้น (`departmentIds` มีแผนกนั้นอยู่) ก็ให้สิทธิ์แล้ว ไม่ได้เช็คว่าเป็น **หัวหน้า** แผนกนั้นจริงๆ
พอมีคนสังกัด 2 แผนก (เช่น หัวหน้าทะเบียนที่ทำงานร่วมกับการเงิน เลย tag ตัวเองอยู่แผนกการเงินด้วย) เขาจะเห็น/ทำงานแทนหัวหน้าแผนกการเงินได้ทันที
ทั้งที่หัวหน้าการเงินตัวจริงยังไม่ได้กด "ส่งต่อ" ให้เลย — แก้ให้ทุกจุดเช็คเฉพาะ "เป็นหัวหน้าแผนกจริง" (`isDeptHeadOf`) เท่านั้น
ตรงกับที่หน้าเว็บ (front-end) ทำถูกอยู่แล้วอยู่แล้ว (ปุ่มรับทราบ/เสร็จสิ้นซ่อนให้คนที่ไม่ใช่หัวหน้าแผนกอยู่แล้ว แต่ backend หลุดให้เปิดดูเอกสารได้)

**ไฟล์ที่แก้:** `server/lib/permissions.js`, `server/server.js` (4 จุด: inbox, acknowledge, complete, แดชบอร์ด)

## 2) งานเสร็จแล้วแต่สถานะเอกสารไม่ปรับเป็น "เสร็จสิ้น"
**สาเหตุที่เจอ:** ตอน "ส่งต่องาน" ถ้าเผลอเลือกทั้ง checkbox แผนก (เช่น "แผนกการเงิน") และเลือกตัวบุคคล
(เช่น "หัวหน้าการเงิน" คนเดียวกัน) พร้อมกันในการส่งครั้งเดียว ระบบจะสร้างเป็นงาน **2 ชิ้นแยกกัน** โดยไม่รู้ตัว
ต้องกดปุ่ม "เสร็จสิ้น" ให้ครบทั้ง 2 ชิ้น เอกสารถึงจะเปลี่ยนสถานะ — ถ้ากดแค่ชิ้นเดียว (นึกว่าเป็นงานเดียวกัน) เอกสารจะค้างที่
"กำลังดำเนินการ" ตลอดไปทั้งที่งานจริงเสร็จแล้ว แก้โดยตัดรายการที่ซ้ำซ้อนกันแบบนี้ออกอัตโนมัติก่อนสร้างงานจริงทุกครั้ง

**ไฟล์ที่แก้:** `server/server.js` (action: forward)

**หมายเหตุ:** เอกสารเก่าที่ค้างสถานะ "กำลังดำเนินการ" อยู่แล้วก่อนแพตช์นี้ ต้องเข้าไปเช็คเองว่ามีงานย่อยไหนที่ยังไม่ได้กด
"เสร็จสิ้น" ค้างอยู่ (ดูได้จากการ์ดใหม่ "สถานะงานที่มอบหมาย" ในหน้ารายละเอียดเอกสาร หรือการ์ด "เอกสารค้างระหว่างดำเนินการนานที่สุด"
ในแดชบอร์ด ผอ.)

## 3) ประวัติดำเนินการ (log) เป็นรหัสภาษาอังกฤษ อ่านยาก
- แปล action ทั้งหมด (created, office_reviewed, endorsed, forwarded, acknowledged, completed ฯลฯ) เป็นภาษาไทย
  ครบทั้ง 3 ภาษาที่ระบบรองรับ (ไทย/อังกฤษ/จีน)
- ใส่ไอคอน + สี ต่างกันตามประเภทเหตุการณ์ ให้กวาดตาดูได้เร็วขึ้น
- เพิ่มการ์ดใหม่ **"สถานะงานที่มอบหมาย"** อยู่เหนือประวัติในหน้ารายละเอียดเอกสาร สรุปให้เห็นทันทีว่า
  ส่งงานถึงใคร/แผนกไหนบ้าง ใครรับทราบแล้ว ใครยังไม่รับทราบ ใครทำเสร็จแล้ว โดยไม่ต้องไล่อ่าน log ทีละบรรทัด
  เหมาะกับที่ ผอ. จะมอนิเตอร์ภาพรวมได้ง่ายขึ้น

**ไฟล์ที่แก้:** `web/src/pages/documents/DocumentDetail.tsx`, `web/src/i18n/dictionaries.ts`

---

ทดสอบแล้ว: `node --check` ฝั่ง server ผ่าน, `tsc -b --force` ฝั่ง web ผ่านสะอาด, และรัน `npm run build` (tsc -b && vite build)
สำเร็จจริงในรอบนี้ (ก่อนหน้านี้ build ไม่ผ่านเพราะ native binding ของ rolldown ขาดหายในเครื่องพัฒนา — รอบนี้ดึงมาลงเพิ่มได้แล้ว)
ไฟล์ที่ build ได้แนบมาให้ใน `server/public/` พร้อมใช้เลย
